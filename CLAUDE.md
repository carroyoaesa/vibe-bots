# CLAUDE.md - contexto denso para Claude Code

Este archivo se carga automáticamente en cada sesión. Objetivo: que tras una auto-compactación no se pierdan reglas operativas críticas ni el estado actual del proyecto. Para detalle completo ver `README.md` (usuarios) y `AGENTS.md` (agentes IA en general).

## Qué es este proyecto

Bot de trading en TypeScript/Node.js (CommonJS, ES2020, `strict: true`) corriendo en una instancia LXD con servicios **nativos** (no Docker): PostgreSQL, Redis, MinIO, Grafana, Prometheus + 3 exporters (Fase 13), todos vía systemd con autostart. Directorio de trabajo: `/root/bots/vibe-bots`. Repo: `github.com/carroyoaesa/vibe-bots`, branch `main`.

Fases: 1 (ingesta ✅), 1.5 (dashboard web ✅), 2 (estrategia + ejecución paper ✅), 3 (snapshots MinIO ✅), 4 (backtesting ✅ + capa de IA con Claude ✅ activa desde 2026-06-14, `ANTHROPIC_API_KEY` configurada), 5 (configuración dinámica: perfil de riesgo + modelo Claude + precios IA-verificados, vía `bot_settings` ✅), 6 (estrategia multi-condicional por símbolo: 12 condiciones de TA en `strategy/conditions.ts`, ganador por símbolo vía `npm run backtest` -> `symbol_conditions`, leído por `runTradingCycle()`/`/api/trading/status` ✅, activa desde 2026-06-15), 6.1 (ampliación de Fase 6, mismo día: `reason` con valores numéricos por condición vía `condition.describe()`, overlays de gráfico por condición en el dashboard vía `CONDITION_CHART_CONFIG`/`app.js`, y dos tablas nuevas en "Resumen por símbolo" - `#signals-summary-table` y `#conditions-table` - ✅, **ambas tablas reemplazadas por el tab "Resumen" desde Fase 9**), 7 (condición activa de **compra** y de **venta** independientes por símbolo: búsqueda de 144 combos `(buyCondition, sellCondition)` = 12x12 `CONDITIONS` vía `npm run backtest` -> `symbol_conditions` con `buy_condition_id`/`sell_condition_id`, consumido por `computeSignal`/`runTradingCycle()`/`/api/trading/status` y reflejado en el dashboard ✅, activa desde 2026-06-15 bajo `exit_mode='signal_only'`, mirror del combo-matrix de `bots/backtests`), 8 (expresiones de condición de 2-3 niveles vía `strategy/conditionExpr.ts` + `strategy/multiConditionOverrides.ts`, y ampliación del watchlist de 20 a 27 símbolos - ✅ **mergeada a `main`** desde 2026-06-17, commit `f74dc9f`, ver sección "Fase 8" más abajo), 9 (clasificación manual por símbolo con hard-block + rediseño del dashboard a tabs - ✅ **mergeada a `main`** desde 2026-06-18, commit `a6052a0`, ver sección "Fase 9"), 10 (operaciones multi-cuenta de solo lectura + backtests segmentados por clasificación - ✅ **mergeada a `main`**, ver sección "Fase 10"), 11 (filtro de candidatos BUY/SELL para la IA -veto real también sobre SELL desde 2026-06-23-, experimento de sesgo A/B/C/D, visibilidad de costo de Claude y limpieza automática de órdenes huérfanas - ✅ **mergeada a `main`** desde 2026-06-21, ver sección "Fase 11"), 12 (alerta por email ante BUY/SELL realmente ejecutados, SMTP genérico vía `nodemailer`, contenido HTML con condiciones técnicas + motivo de IA + gráfico PNG vía `chartjs-node-canvas` - ✅ desde 2026-06-22, ver sección "Fase 12"), 13 (monitoreo de infraestructura: Grafana reactivado + Prometheus + node/postgres/redis exporters, dashboard "Vibe Bots - Infra" embebido en el tab Sistema vía iframe - ✅ desde 2026-06-22, ver sección "Fase 13").

**Fase híbrido** (2026-06-15, branch `hibrido`): ⚠️ **el commit base de esta branch (`2331bfd`) terminó mergeado a `main` en algún momento (es ancestro directo de `main`, verificado 2026-06-21 con `git merge-base --is-ancestor`), pero la ORQUESTACIÓN que describe esta sección (Pasada 1 Tier 1 in-place, Pasada 1b paralelo/sombra) NO está conectada en el `runTradingCycle()` actual** - el loop principal de señales llama a `computeSignal()` (1D) sin ninguna rama a `computeSignal1H()` para ningún símbolo. El código de soporte (`hybridConfig.ts`, `conditions1h.ts`, `parallelStore.ts`, `computeSignal1H`) sigue existiendo y compilando, y SÍ se usa, pero solo para un pick informativo 1H dentro de `runBacktestForSymbols()` (`backtestRunner.ts`, guardado como fila adicional `timeframe='1Hour'` en `symbol_conditions`, **no alimenta el resumen de portafolio ni señales/órdenes reales**) y para el chart opcional `?tf=1H` del dashboard. Tratar todo lo descrito más abajo en "Fase híbrido" como **arquitectura disponible pero no en producción**, no como comportamiento de trading real - si se quiere reactivarla, hay que volver a cablear la Pasada 1/1b en `tradingRunner.ts` explícitamente. Ver sección "Fase híbrido" más abajo para el diseño original.

## Reglas operativas críticas (NO romper sin pedir confirmación explícita)

1. **`src/server.ts` (dashboard web) NUNCA va como servicio systemd.** Se gestiona solo con `./scripts/start-web.sh`, `./scripts/stop-web.sh`, `./scripts/status.sh` (nohup + PID en `run/web.pid`, logs en `logs/web.log`). Decisión deliberada del usuario, repetida más de una vez.
2. **`runTradingCycle()` (`npm run trade`, `POST /api/trading/run`, botón "Ejecutar ciclo de trading") coloca/cierra órdenes REALES en la cuenta PAPER de Alpaca.** No hay modo dry-run separado, aunque desde 2026-06-14 el interruptor ON/OFF del dashboard (`bot_settings.trading_enabled`, default `true`) se le acerca: en `false`, bloquea TODAS las compras/ventas (`TRADING_DISABLED`) sin afectar el cálculo de señales/evaluaciones de IA - ver "Configuración dinámica (Fase 5)". Cualquier cambio a `tradingRunner.ts`, `strategy/config.ts`, `strategy/signals.ts`, `strategy/conditions.ts`, `strategy/backtest.ts`, `bot_settings` (Fase 5) o `symbol_conditions` (Fase 6) es un cambio de comportamiento de trading real (en paper). **Desde 2026-06-14 corre automáticamente vía cron** (`npm run trade:cron`, ver "Automatización (cron)" más abajo) **cada 5 minutos** durante el horario de mercado (no cada hora - cadencia corregida 2026-06-21, ver "Automatización") - no es necesario ejecutarlo manualmente salvo pedido explícito. **Desde Fase 4**, `runTradingCycle()` también corre una fase de evaluación con Claude (desde Fase 11, limitada a candidatos BUY y SELL que realmente importan -ver "Fase 11"- BUY-only hasta 2026-06-22, ampliada a SELL con veto real desde 2026-06-23) que puede vetar señales BUY (`AI_BLOCKED`) o SELL (`AI_BLOCKED_SELL`) y, desde Fase 5, puede ajustar `estimatedEntryPrice`/`estimatedExitPrice` (acotado a ±10%) - ver "Capa de IA (Claude, Fase 4/5)" y "Configuración dinámica (Fase 5)" más abajo. `ANTHROPIC_API_KEY` está configurada (desde 2026-06-14): la fase de IA corre normalmente. Sigue siendo **fail-open**: si la llamada a Claude falla por cualquier motivo, esa fase se omite (warning logueado) y el ciclo continúa con el perfil de riesgo/modelo de `bot_settings` y los precios algorítmicos.
3. **Push a `main` en GitHub requiere confirmación explícita del usuario en cada ocasión** (no asumir autorización previa).
4. **No instalar paquetes apt a nivel de sistema** (p.ej. `playwright install-deps`) sin pedido explícito - se considera modificación significativa de infraestructura compartida.
5. Secretos en `secure/keys.env` (o `.env` local), nunca en el repo. Credenciales de Git van vía `~/.git-credentials` (credential helper), no en `secure/keys.env` ni en la URL del remoto.
6. Si se edita `/etc/grafana/grafana.ini` (fuera del repo), restaurar `chown root:grafana` y `chmod 640` después.

## Mapa del repo

- `src/index.ts`, `src/check-runner.ts`, `src/diagnostics.ts` - 10 health checks (`npm run dev`, `GET /api/health`), incluyendo `anthropic` (Fase 4, ✅ desde que se configuró `ANTHROPIC_API_KEY`).
- `src/ingest.ts`, `src/ingestRunner.ts` - ingesta Fase 1 (`npm run ingest`, `POST /api/ingest`).
- `src/watchlist.ts` - **fuente única de verdad**: `WATCHLIST` (27 símbolos desde Fase 8), `ETF_SYMBOLS` (12, subconjunto de WATCHLIST), `MACRO_SERIES`, `BARS_LOOKBACK_DAYS=220`.
- `src/strategy/` - lógica pura, sin I/O:
  - `indicators.ts`: `sma`, `ema*`, `rsi`/`rsiSeries`, `macdSeries`, `bollingerBands`, `stochasticSeries`, `williamsRSeries`, `cciSeries`, `priorHighSeries`/`priorLowSeries`, `momentum`, `smaSeries`, `estimateEntryPrice`.
  - `conditions.ts` (Fase 6): `OhlcBar` (`{ts, open, high, low, close}`), `IndicatorContext`, `buildIndicatorContext(bars)`, catálogo `CONDITIONS` (12 condiciones de TA, cada una `{id, label, evaluate(ctx, i), describe(ctx, i)}` - `describe` devuelve los valores de indicador que justifican la señal, usados en `reason`, Fase 6.1), `DEFAULT_CONDITION_ID = 'sma_cross_10_30'`, `computeEstimatedEntryPrice(ctx, i, conditionId)`.
  - `conditions1h.ts` (Fase híbrido, branch `hibrido`): `SCALE_1H = 8`, `buildIndicatorContext1H(bars)` (igual que `buildIndicatorContext` pero todos los períodos x8) y `computeEstimatedEntryPrice1H(ctx, i, conditionId)`; `MIN_BARS_1H = 401` (= `50*SCALE_1H + 1`, para que SMA50 reescalada esté disponible). `CONDITIONS`/`evaluate`/`describe` no cambian - leen los mismos nombres de campo del `IndicatorContext`.
  - `hybridConfig.ts` (Fase híbrido, branch `hibrido`): `HYBRID_CONFIG: Record<string, {tier: 1|2|'shadow', buyConditionId, sellConditionId}>` (SPY/XLU=tier1, MS/QQQM=tier2, SCHD='shadow'), `HYBRID_SYMBOLS`/`TIER1_SYMBOLS`/`TIER2_SYMBOLS`/`SHADOW_SYMBOLS` (derivados), `PARALLEL_RISK_PROFILE` (Tier 2: 5% equity/posición, sin TP/SL, máx. 2 posiciones paralelas).
  - `conditionExpr.ts` (Fase 8): parser/evaluador de EXPRESIONES de condición (no solo un id de `CONDITIONS`) - soporta `idA` (trivial), `idA&idB`/`idA|idB` (2 cond.), `AND(idA+idB+idC)`/`OR(idA+idB+idC)` y formas mixtas `(idA&idB)|idC`/`(idA|idB)&idC` (3 cond., igual sintaxis que produce `bots/backtests/src/runMultiCondMatrix.ts`/`runTripleCondMatrix.ts#shapeLabel`). `parseConditionExpr(raw)` (fail-loud: lanza si hay un id desconocido o sintaxis inválida - un typo es un bug de autoría estática, no se absorbe en un fallback silencioso), `evaluateConditionExpr(node, ctx, i, action)`, `leafIds(node)`, `describeConditionExpr(node, ctx, i, action?)` (compone `describe()` de cada hoja; si la expresión tiene 2-3 hojas y se pasa `action`, antepone `→ ` a la descripción de la hoja cuyo `evaluate(ctx,i) === action` - marca cuál disparó realmente, agregado 2026-06-23, ver "Fase 11"), `labelConditionExpr(raw)` (versión humana, sustituye ids por `.label` y operadores por "Y"/"O"), `estimateConditionExprEntryPrice(node, ctx, i, computeEntryPrice)` (promedia `computeEntryPrice` sobre las hojas que dispararon BUY, o todas si ninguna lo hizo - para 1 sola hoja es idéntico al valor de Fase 7).
  - `multiConditionOverrides.ts` (Fase 8): `MULTI_CONDITION_OVERRIDES: Record<string, {tier: 2|3, buyExpr, sellExpr}>` (26 símbolos - todo `WATCHLIST` menos `SOXQ`), basado en `bots/backtests/output/report_data.json` (reporte 2026-06-17). Se auto-valida al importarse (parsea todas las expresiones con `parseConditionExpr`, lanza si alguna falla). Ver sección "Fase 8" más abajo para el detalle completo (qué símbolos se reincorporaron, cuáles quedaron afuera y por qué).
  - `chartOverlays.ts` (Fase 12): `CONDITION_CHART_CONFIG`/`mergeConditionChartConfig(buyConditionId, sellConditionId)` - puerto a TypeScript de la config homónima de `public/app.js`, usado por `services/chartImage.ts` para el gráfico embebido en la alerta de email. Ver "Fase 12" para el detalle de por qué está duplicado en vez de compartido con el frontend.
  - `signals.ts`: `computeSignal(symbol, bars: OhlcBar[], riskProfile = RISK_PROFILE, buyConditionId = DEFAULT_CONDITION_ID, sellConditionId = DEFAULT_CONDITION_ID, exitMode: ExitMode = 'bracket') -> SignalResult` (Fase 7: evalúa `buyCondition`/`sellCondition` por separado - `signal = 'BUY'` si `buyCondition.evaluate(...)==='BUY'`, `'SELL'` si `sellCondition.evaluate(...)==='SELL'`, si no `'HOLD'`; incluye `buyConditionId`/`buyConditionLabel`/`sellConditionId`/`sellConditionLabel`, ya no `conditionId`/`conditionLabel`; `exitMode` agregado en la Fase 8.1, ver "Modelo de precios de la estrategia" más abajo). `reason` compone `describeConditionExpr(expr, ctx, i, action)` por lado (Fase 11, 2026-06-23) - marca con `→ ` la hoja que realmente disparó cuando hay 2-3 condiciones. Fase híbrido (branch `hibrido`): la lógica común se extrajo a `computeSignalWith(...)`; `computeSignal1H(symbol, bars1h, riskProfile, buyConditionId, sellConditionId, exitMode)` es el mismo cálculo pero con `buildIndicatorContext1H`/`computeEstimatedEntryPrice1H`/`MIN_BARS_1H` (`conditions1h.ts`).
  - `config.ts`: `STRATEGY_PARAMS` (SMA 10/30, RSI14, umbral 70, momentum 10 - usados por `sma_cross_10_30`), `RiskProfile` (tipo), `RISK_PROFILE` (perfil "moderado": 10% equity/posición, SL -3%/TP +6%, máx 5 posiciones) y `RISK_PROFILE_PRESETS` (conservador/moderado/agresivo) - **desde Fase 5, solo defaults/semillas**; la fuente de verdad en runtime es `bot_settings`.
  - `chart.ts` (Fase 6.1, reescrito): `buildChartSeries(bars: OhlcBar[]) -> ChartPoint[]` - expone TODOS los campos de `IndicatorContext` (sma10/20/30/50, ema12/26, rsi14, macd/macdSignal, bbUpper/Middle/Lower, stochK/D, williamsR, cci20, priorHigh20/priorLow10) para `/api/trading/chart/:symbol`, que ahora usa `getRecentOhlcBars` + `CHART_LOOKBACK_BARS=150` (antes `getRecentBars` + 90). El frontend elige overlays por símbolo según su condición activa (`CONDITION_CHART_CONFIG` en `app.js`).
- `src/services/` - I/O: `alpaca.ts` (incluye `AccountGroup`/`ACCOUNT_GROUPS`/`getAlpacaClient(group)`, Fase 10), `marketData.ts` (incluye `getLatestQuotes(client, symbols)`, Fase 11, 2026-06-23 - cotización en vivo batcheada para decisiones de orden, ver "Fase 11"), `marketStore.ts` (incluye `getRecentOhlcBars`, Fase 6), `tradingStore.ts`, `backtestStore.ts`, `settingsStore.ts` (Fase 5, `bot_settings`), `conditionStore.ts` (Fase 6, `symbol_conditions`), `symbolClassificationStore.ts` (Fase 9, `symbol_classifications` - caché en memoria TTL 30s), `preTradeCheck.ts` (Fase 10, `canPlaceBuyOrder()` - gate unificado de BUY, caché 30s por grupo+símbolo), `operationsSync.ts` (Fase 10, poller multi-cuenta - `account_state`/`*_snapshot`), `staleOrders.ts` (Fase 11, limpieza de órdenes BUY huérfanas), `claudeExperimentStore.ts` (Fase 11, `claude_gate_experiment`), `claudeUsageStore.ts` (Fase 11, `claude_usage_log`), `email.ts` (Fase 12, `sendTradeAlertEmail()` vía `nodemailer`, HTML + gráfico inline), `chartImage.ts` (Fase 12, `renderSymbolChartPng()` vía `chartjs-node-canvas`/`canvas`), `claude.ts`, `db.ts`, `cache.ts`, `storage.ts`, `fmp.ts`, `finnhub.ts`, `alphaVantage.ts`, `fred.ts`.
  - Fase híbrido (branch `hibrido`, ⚠️ orquestación inactiva en producción, ver nota arriba): `marketData.ts` agrega `getHourlyBars(client, symbols, days)` (igual que `getDailyBars` pero `timeframe='1Hour'` - **requiere un cliente de `createMarketDataClient`**, no `createAlpacaClient`, ver "Fase híbrido" más abajo); `marketStore.ts` agrega `saveHourlyBars`/`getRecentOhlcBars1H` (mismo `market_bars`, `timeframe='1Hour'`); `alpaca.ts` agrega `closePositionQty(client, symbol, qty)` (`DELETE /v2/positions/:symbol?qty=N`, cierre parcial); `tradingStore.ts` agrega columnas `system`/`timeframe` + `getLatestHybridSignals`; nuevo `parallelStore.ts` (`setupParallelSchema`, tabla `parallel_positions`, CRUD de posiciones del sistema paralelo Tier 2). Estos módulos siguen compilando y `marketStore`/`tradingStore` siguen usándose por otras fases (Fase 10 reusa `system`/`timeframe`, `account_group` se agregó al lado de ellas) - lo inactivo es solo la orquestación de señales 1H en `tradingRunner.ts`.
- `src/strategy/backtest.ts` - `runCombinedBacktest(symbol, bars: OhlcBar[], riskProfile = RISK_PROFILE, buyConditionId = DEFAULT_CONDITION_ID, sellConditionId = DEFAULT_CONDITION_ID, exitMode: ExitMode = 'bracket') -> BacktestResult` (Fase 7), lógica pura: entradas vía `buyCondition.evaluate`/`computeEstimatedEntryPrice(ctx, i, buyConditionId)`, salidas por señal vía `sellCondition.evaluate(...)==='SELL'` (más TP/SL si `exitMode='bracket'`, Fase A.1). `runBacktest(symbol, bars, riskProfile, conditionId, exitMode)` (Fase 4, generalizado a las 12 condiciones en Fase 6) es ahora un wrapper: `runCombinedBacktest(symbol, bars, riskProfile, conditionId, conditionId, exitMode)`.
- `src/backtestRunner.ts`, `src/backtest.ts` - `runBacktestForWatchlist(pool)` + CLI (`npm run backtest`, Fase 4).
- `src/backfillHistory.ts` - CLI opcional (`npm run backfill-history`, Fase 4, no corrido aún).
- `src/backfillHourlyHistory.ts` - CLI opcional (`npm run backfill-1h`, no corrido aún) - histórico de velas 1H para `HYBRID_SYMBOLS`, alimenta el pick informativo de `backtestRunner.ts` (ver nota "Fase híbrido" arriba).
- `src/tradingRunner.ts` - `runTradingCycle()`, lógica compartida CLI/web. Desde Fase 4 incluye la fase de evaluación IA (Claude), limitada a candidatos relevantes desde Fase 11 (BUY-only hasta 2026-06-22, BUY+SELL desde 2026-06-23), que puede vetar BUYs (`AI_BLOCKED`) o SELLs (`AI_BLOCKED_SELL`). Desde Fase 10, el pre-trade check de BUY pasa por `preTradeCheck.ts` (clasificación/posición/orden pendiente/exposición/máx. posiciones, fail-fast) en vez de checks sueltos.
- `src/server.ts` - Express, puerto `WEB_PORT` (4000). Sirve `public/` + `/api/*`.
- `public/` - frontend estático (`index.html`, `app.js`, `styles.css`), Chart.js v4 vía CDN.
- `grafana/dashboards/*.json` - dashboards provisionados vía API (`admin:admin`), incluye `vibe-infra.json` desde Fase 13.
- `monitoring/` (Fase 13): `collect-vibebots-metrics.sh` (cron cada 5 min, métricas custom de espacio para `node_exporter`) y `postgres_exporter_queries.yaml` (config custom de `postgres_exporter`, copiar a `/etc/prometheus-postgres-exporter/queries.yaml` en la VM) - ver "Fase 13".
- `scripts/` - `start-web.sh`, `stop-web.sh`, `status.sh` (este último, desde Fase 13, también chequea `prometheus`/los 3 exporters).

## Watchlist (27 símbolos, `src/watchlist.ts`)

- **ETFs (12)**: `SPY, SCHE, SCHF, XLP, XLU, XMMO, VUG, SCHD, SPMO, QQQM, SOXQ, SCHG`.
- **Acciones (15)**: `AAPL, MSFT, NVDA, REG, TOL, AMZN, TSM, GOOGL, MS, AVGO, HD, LOW, MAIN, MU, SHW`.
- Desde Fase 9, el dashboard muestra los 27 símbolos como filas de la tabla del tab "Resumen" (sin agrupar por ETF/Acción ni rankear por `attractivenessScore` - eso era de la UI vieja de cards, reemplazada). El detalle por símbolo (gráfico, IA, backtest) vive en el tab "Detalle", a un click desde Resumen.
- Reducido desde 28 a 20 símbolos (2026-06-14) tras un análisis de backtests/correlación/liquidez: se quitaron `NECB, DBEZ, PPA, AVGO, MU, AGM` por baja probabilidad de retorno con la estrategia actual (sin señales, micro-caps ilíquidos, o volatilidad diaria que supera el SL fijo del 3%); luego `QQQ` (duplicado casi perfecto de `QQQM`, r=0.999, comisión más alta: 0.20% vs 0.15%) y `SCHG` (duplicado casi perfecto de `VUG`, r=0.993, comisión más alta: 0.04% vs 0.03%).
- **Ampliado de 20 a 27 (Fase 8, 2026-06-17)**: se reincorporaron `AVGO, HD, LOW, MAIN, MU, SCHG, SHW` tras el reporte de 3 niveles de `bots/backtests` (señal "usar" con 3 condiciones + volumen diario líquido, ver sección "Fase 8"). `QQQ` sigue excluido de forma **permanente** (no reabrir - mismo motivo que 2026-06-14). `GOLD, AGM, DBEZ, NECB, PPA` también tenían señal "usar" en ese reporte pero NO se agregaron - ver "Fase 8" para el detalle de por qué cada uno quedó afuera.

## Modelo de precios de la estrategia (estado actual)

- `estimatedEntryPrice` (`SignalResult`, vía `computeEstimatedEntryPrice(ctx, i, conditionId)` en `strategy/conditions.ts`, Fase 6):
  - `sma_cross_10_30`/`sma_cross_20_50`: nivel de cierre que haría que la SMA rápida (próxima sesión) alcance la SMA lenta actual (`estimateEntryPrice`, parametrizado por período).
  - `ema_cross_12_26`/`macd_cross`: proyección analítica real (resuelve la fórmula de actualización EMA para el precio que igualaría EMA12/EMA26 o MACD/señal en la próxima barra) - acotada a `0 < p < price*4`, si no cae a `price`.
  - `stochastic_cross`/`williams_r_reversal`/`cci_reversal`: precio que llevaría el oscilador exactamente al umbral de activación, usando el rango high/low del período.
  - `bollinger_reversion`→`bbLower[i]`, `bollinger_breakout`→`bbUpper[i]`, `donchian_breakout_20`→`priorHigh20[i]`, `trend_pullback_sma50`→`sma50[i]`: nivel de indicador, no proyección a futuro.
  - `rsi_reversal_30_70`: `price` (cierre actual) - el mapeo RSI→precio es path-dependent (depende de avgGain/avgLoss históricos), no se intenta proyectar.
  - `null` solo si histórico insuficiente (< 51 velas, ver "Backtesting (Fase 4/6)").
  - ⚠️ **Auditoría 2026-06-17** (Fase 8.1): para `bollinger_reversion`/`bollinger_breakout`/`donchian_breakout_20`/`trend_pullback_sma50` el nivel usado puede quedar significativamente por debajo del precio de mercado en el momento en que la señal dispara - porque la señal misma exige que el precio ya haya cruzado ese nivel (reversión: precio ya rebotó por ENCIMA de `bbLower`; breakout/pullback: precio ya está por ENCIMA de `bbUpper`/`priorHigh20`/`sma50`). Confirmado con datos reales de `trading_orders`/`trading_signals` (8 órdenes `pending_new`, 0 `filled`): GOOGL con `trend_pullback_sma50` activo - `estimated_entry_price=365.21` vs. `price` de la señal `373.37` (gap de -2.2%); MS con `cci_reversal` - gap de -2.6%; AMZN con `cci_reversal` - gap de -2.16%. Una orden LÍMITE puesta ahí puede tardar mucho en llenarse o no llenarse nunca (pide volver a un nivel que el propio momentum del breakout/pullback puede no repetir). **Decisión 2026-06-23: no corregir, a propósito** (ver "Fase 11" más abajo, bullet "Decisión explícita del usuario sobre el precio estimado de entrada") - el usuario prefiere el nivel de indicador puro (no `price` ni un intermedio) incluso si la orden nunca llena, pensando en cuando se use margen y no se quiera pagar precio premium. Lo que se agregó en su lugar es transparencia: `describeConditionExpr` marca con `→ ` cuál hoja fue la que realmente disparó cuando hay 2-3 condiciones, para que no quede ambiguo de dónde sale el nivel.
  - `strategy/conditionExpr.ts#estimateConditionExprEntryPrice` (Fase 8): para expresiones de 2-3 condiciones, promedia el estimado de las hojas que dispararon BUY. El promedio es razonable cuando las hojas activas están en magnitudes similares, pero NO corrige el problema de arriba si la(s) hoja(s) activa(s) son alguna de las 4 cuestionadas - heredan el mismo sesgo. No se cambió el criterio (no hay evidencia de que mínimo/máximo/mediana sea claramente mejor sin antes corregir el problema de base por condición) - revisar junto con el punto anterior.
- `estimatedExitPrice` (`SignalResult`): `estimatedEntryPrice * (1 + riskProfile.takeProfitPct)`, **solo si `exitMode === 'bracket'`** (`riskProfile`/`exitMode` = `bot_settings`, Fase 5). `null` si `estimatedEntryPrice` es `null` o si `exitMode !== 'bracket'`. ⚠️ **Bug corregido 2026-06-17 (Fase 8.1)**: antes de esta corrección, `computeSignal`/`computeSignal1H` no recibían `exitMode` como parámetro y calculaban este valor siempre que `riskProfile.takeProfitPct > 0`, sin importar el modo de salida - con `exit_mode='signal_only'` (preset activo desde Fase 7) y `take_profit_pct=0.06` (valor guardado en `bot_settings`, no usado para colocar órdenes en este modo pero tampoco puesto en 0), el dashboard mostraba un "Precio est. salida" de TP teórico que ninguna orden real usa (la salida en `signal_only` es únicamente por señal SELL). Confirmado en vivo el 2026-06-17 vía `/api/trading/status` antes y después del fix. `computeSignal(symbol, bars, riskProfile, buyConditionId, sellConditionId, exitMode = 'bracket')` y `computeSignal1H(...)` ahora reciben `exitMode`; los 2 call sites (`tradingRunner.ts`, `server.ts` `/api/trading/status`) pasan `settings.exitMode`. La columna "P. salida est." se quitó de `#signals-summary-table` (siempre vacía mientras el preset activo sea `signal_only`) - se mantiene en el detalle por símbolo y en el gráfico (ya degradaban a `—`/sin línea correctamente). `claude.ts#buildPrompt` ya mostraba `'n/a'` cuando es `null` - sin cambios ahí, pero ahora el contexto que recibe Claude es honesto (no más "precio de salida" inexistente).
- **Fase 5**: en `runTradingCycle`, antes de `saveSignal`, si la fase de IA propuso `adjustedEntryPrice`/`adjustedExitPrice` y ambos quedan dentro de ±10% del valor algorítmico (y `adjustedExit > adjustedEntry`), se sobrescriben `signal.estimatedEntryPrice`/`estimatedExitPrice` con esos valores (`applyPriceAdjustment` en `tradingRunner.ts`). Si no, se mantienen los algorítmicos.
- En `runTradingCycle`, BUY coloca una orden LÍMITE (`type: 'limit'`, `limit_price = estimatedEntryPrice`); si `bot_settings.exit_mode = 'bracket'` (default), es una **bracket order** con `take_profit.limit_price = estimatedExitPrice` y `stop_loss.stop_price = estimatedEntryPrice * (1 - riskProfile.stopLossPct)` - ya reflejando el ajuste de IA si aplicó; si `exit_mode = 'signal_only'`, es una orden simple sin TP/SL (ver "Fase A.1" en "Configuración dinámica"). La cantidad (`qty`) se sigue calculando sobre `equity * riskProfile.positionSizePct / signal.price` (precio de mercado actual) en ambos casos.
- Ambos precios se muestran en las tablas del dashboard ("Precio est. entrada" / "Precio est. salida") y como líneas horizontales punteadas en los gráficos por símbolo (amarillo = entrada, violeta = salida). `GET /api/trading/status` muestra el último valor persistido en `trading_signals` (= verificado/ajustado por IA en el ciclo más reciente), con fallback al recién calculado si no hay fila aún.

## DB - columnas relevantes en `trading_signals`

`symbol, ts, price, sma_fast, sma_slow, rsi, momentum, estimated_entry_price, estimated_exit_price, signal, reason, buy_condition_id, buy_condition_label, sell_condition_id, sell_condition_label` (las últimas 4, Fase 7, `TEXT` nullable - reemplazan `condition_id`/`condition_label` de Fase 6), más `system`/`timeframe` (ver abajo) y `account_group` (Fase 10, `TEXT NOT NULL DEFAULT 'aptos'`, deriva de `symbol_classifications` vía `classificationToAccountGroup()` - solo etiqueta, no rutea la orden real). Migraciones vía `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` (nuevas) + `ALTER TABLE ... DROP COLUMN IF EXISTS` (viejas) en `setupTradingSchema` (`tradingStore.ts`), ejecutado al inicio de `runTradingCycle()` pero NO por `GET /api/trading/status` - si agregás/renombrás una columna, asegurate de que ya exista en la DB (correr `npm run trade` una vez, o un script puntual que llame `setupTradingSchema`) antes de que ese endpoint la consulte.

- `system TEXT NOT NULL DEFAULT 'main'` (`'main' | 'parallel' | 'shadow'`) y `timeframe TEXT NOT NULL DEFAULT '1Day'` (`'1Day' | '1Hour'`) en `trading_signals`, y `system TEXT NOT NULL DEFAULT 'main'` (`'main' | 'parallel'`) en `trading_orders` - migración de la Fase híbrido, **ya aplicada en producción** (la migración corre en cada `runTradingCycle()`, que viene corriendo por cron desde antes de los merges de Fase 8/9/10/11 - el "pendiente" de versiones anteriores de esta nota ya no aplica). `getLatestSignals` filtra `WHERE system = 'main'`. En la práctica, todo símbolo persiste hoy con `system='main', timeframe='1Day'` (ver nota "Fase híbrido" arriba - la orquestación que pondría algún símbolo en `'parallel'`/`'shadow'`/`'1Hour'` no está conectada).

## `symbol_conditions` (Fase 6/7, tabla singleton-por-símbolo)

`src/services/conditionStore.ts`: `setupConditionSchema(pool)` crea/migra `symbol_conditions (symbol TEXT PRIMARY KEY, buy_condition_id TEXT NOT NULL DEFAULT 'sma_cross_10_30', buy_condition_label TEXT NOT NULL DEFAULT 'Cruce SMA10/SMA30 + RSI<70 + Momentum>0', sell_condition_id TEXT NOT NULL DEFAULT 'sma_cross_10_30', sell_condition_label TEXT NOT NULL DEFAULT 'Cruce SMA10/SMA30 + RSI<70 + Momentum>0', trades INTEGER NOT NULL, win_rate_pct NUMERIC, total_return_pct NUMERIC, avg_return_pct NUMERIC, max_drawdown_pct NUMERIC, updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW())`. Fase 7 reemplaza `condition_id`/`condition_label` (Fase 6) por las 4 columnas buy/sell vía `ADD COLUMN IF NOT EXISTS ... DEFAULT` (backfillea filas existentes) + `DROP COLUMN IF EXISTS` de las viejas. `saveSymbolConditions(pool, picks)` (upsert de 10 columnas por `symbol`, llamado desde `runBacktestForWatchlist`) y `getSymbolConditions(pool) -> Map<symbol, SymbolConditionRow>` (con `buyConditionId/buyConditionLabel/sellConditionId/sellConditionLabel`, leído sin caché por `runTradingCycle()` y `GET /api/trading/status`, igual patrón que `bot_settings`). Fallback si no hay fila: `DEFAULT_CONDITION_ID = 'sma_cross_10_30'` para ambas (= comportamiento histórico).

## `/api/trading/status` (importante para no romper)

Recalcula señales **frescas** (no cacheadas) para los 27 símbolos vía `getRecentOhlcBars(pool, symbol, BARS_LOOKBACK=100)` + `computeSignal`, etiquetando cada una con `type: 'ETF' | 'STOCK'` según `ETF_SYMBOLS`. Antes, lee `symbolConditions = getSymbolConditions(pool)` (sin caché, Fase 6) y pasa `buyConditionId = MULTI_CONDITION_OVERRIDES[symbol]?.buyExpr ?? symbolConditions.get(symbol)?.buyConditionId ?? DEFAULT_CONDITION_ID` / análogo para `sellConditionId` (Fase 8: el override de 2-3 condiciones tiene precedencia sobre el pick de 1 condición) a `computeSignal` (Fase 7). `getLatestSignals` (`tradingStore.ts`) se usa solo para sobrescribir `estimatedEntryPrice`/`estimatedExitPrice` con el último valor persistido (verificado/ajustado por IA).

`GET /api/conditions` (Fase 6/7, +B&H Fase A, +overrideTier Fase 8): `{ ok, generatedAt, conditions: [{symbol, buyConditionId, buyConditionLabel, sellConditionId, sellConditionLabel, trades, winRatePct, totalReturnPct, avgReturnPct, maxDrawdownPct, updatedAt, buyHoldReturnPct, overrideTier}], catalog: CONDITIONS.map(c => ({id, label})), buyHoldPeriod: {start, end} | null }` - condición activa de **compra** y de **venta** + métricas por símbolo (desde `symbol_conditions`, fallback `sma_cross_10_30` para ambas/`trades: 0`) y catálogo completo de las 12 condiciones. Si el símbolo tiene entrada en `MULTI_CONDITION_OVERRIDES` (Fase 8), `buyConditionId`/`buyConditionLabel`/`sellConditionId`/`sellConditionLabel` reflejan la expresión del override (vía `labelConditionExpr`), NO la fila de `symbol_conditions` - pero `trades`/`winRatePct`/`totalReturnPct`/`maxDrawdownPct` siguen siendo los de la última corrida de `npm run backtest` (1 condición, tier 1), que no recalcula el override; `overrideTier: 2 | 3 | null` le avisa al frontend que esas métricas no son las del combo activo. `buyHoldReturnPct` (Fase A, 2026-06-15): retorno de comprar y mantener durante `buyHoldPeriod` (= `start_date`/`end_date` del último `backtest_runs`), **con dividendos reinvertidos** - vía `getAdjustedCloses` (`services/marketData.ts`, `adjustment=all` de Alpaca, solo para esta comparación, no toca `market_bars`). Columna "Buy & Hold" en `#conditions-table`. Best-effort: si la llamada a Alpaca falla, `buyHoldReturnPct: null` y `buyHoldPeriod: null` sin romper el resto del endpoint.

**Fase 6.1** (mismo día, ampliación cosmética de Fase 6 - sin cambios en `evaluate()`/precios): `reason` (`trading_signals.reason` y `/api/trading/status`) ahora incluye los valores de indicador de la condición activa vía `condition.describe()`, p.ej. `` BUY por "Cruce MACD(12,26,9) / Señal" (MACD=1.234 Señal=0.987) ``. Dashboard ("Resumen por símbolo"): tabla `#signals-summary-table` ("Resumen de señales", 20 filas con condición activa + motivo) y tabla `#conditions-table` ("Condiciones por símbolo (backtest)", desde `/api/conditions`); gráficos por símbolo (`/api/trading/chart/:symbol`) muestran overlays específicos de la condición activa vía `CONDITION_CHART_CONFIG` (`app.js`).

**Fase 7** (2026-06-15, basada en el combo-matrix de `/root/bots/backtests` (proyecto standalone) - 27/28 símbolos mejoran usando condiciones de compra/venta distintas bajo `exit_mode='signal_only'`): la condición activa de **compra** y la de **venta** ahora son independientes por símbolo (antes una sola `conditionId`/`conditionLabel` compartida, Fase 6). `computeSignal` evalúa `buyCondition.evaluate`/`sellCondition.evaluate` por separado (`signal='BUY'` si `buyCondition` da BUY, `'SELL'` si `sellCondition` da SELL, si no `'HOLD'`); `reason` distingue el caso misma-condición (`` Sin señal (condición activa: "X"; ...) ``) del caso cruzado (`` Sin señal (compra: "X" ...; venta: "Y" ...) ``). `claude.ts#buildPrompt` muestra `Condición activa: X` (si compra=venta) o `Condición de compra: X | Condición de venta: Y` (si difieren). Dashboard: `#signals-summary-table` y `#conditions-table` tienen columnas separadas "Cond. compra"/"Cond. venta" (antes una sola "Condición"); los gráficos por símbolo combinan los overlays de ambas condiciones vía `mergeConditionChartConfig(buyConditionId, sellConditionId)` (`app.js` - une los arrays `price` de `CONDITION_CHART_CONFIG` por `key` sin duplicar, y prioriza el `oscillator` de la condición de compra). Verificado end-to-end 2026-06-15: `npm run backtest` (run id 21, ej. NVDA +251.91%, TSM +277.24%, SOXQ +254.18%) y `/api/conditions`/`/api/trading/status` vía curl - los 20 símbolos del watchlist quedaron con condición de compra distinta de la de venta (100% cruzadas), consistente con el hallazgo del combo-matrix.

`account`/`positions` vienen de Redis vía `getCachedOrFetch` (`alpaca:account` TTL 45s, `alpaca:positions` TTL 30s, `src/services/cache.ts`) - si no hay caché, se piden a Alpaca y se cachean. `openOrdersCount`/`openOrdersAt` se leen **solo** de `alpaca:open-orders` (TTL ~70min, sin fetch propio); valen `null` si `runTradingCycle()` no corrió desde el último reinicio de Redis. Esto es puramente para no repetir llamadas a Alpaca en el polling de 60s del dashboard - **nunca** se usa para decisiones de trading (`runTradingCycle()` siempre pide datos frescos).

## Caché en Redis (2026-06-14)

`src/services/cache.ts`: `getCachedJson`/`setCachedJson`/`getCachedOrFetch` (JSON + `cachedAt`, TTL vía `EX`). Usos:
- `quote:<SYMBOL>` (TTL 5min, `npm run ingest`, sin cambios).
- `health:<id>` para los checks de `runDiagnostics()` que llaman APIs externas (`market-data`/`finnhub` 5min, `fmp` 10min, `fred` 30min, `anthropic` 10min, **`alpha-vantage` 2h** - su free tier es 25 req/día y sin esto el polling de 60s de `/api/health` lo agotaba en ~25min). Si hay hit, NO se llama a la API y el resultado trae `cached: true`/`cachedAt`; los fallos no se cachean (se reintenta en el próximo poll). `postgres`/`redis`/`minio` nunca se cachean.
- `alpaca:account`/`alpaca:positions`/`alpaca:open-orders` - ver bullet de `/api/trading/status` arriba. `alpaca:account` se comparte entre el check `alpaca` de `/api/health` y `/api/trading/status` (misma clave/TTL).

## Backtesting (Fase 4, multi-condicional desde Fase 6)

- `src/strategy/backtest.ts` (`runCombinedBacktest(symbol, bars, riskProfile, buyConditionId, sellConditionId, exitMode)`, lógica pura, Fase 7): simula un PAR `(condición de compra, condición de venta)` - misma regla de entrada límite `min(estimatedEntryPrice, price)` vía `buyCondition`, fill al día siguiente vía `low <= entryPrice` - sobre el histórico de `market_bars` de un símbolo, en % de retorno (sin equity/cash compartido ni cap de posiciones - eso sería v2). Salida según `exitMode` (Fase A.1, default `'bracket'`): con `'bracket'`, TP/SL del `riskProfile` activo (igual que antes); con `'signal_only'`, sin TP/SL - solo señal SELL de `sellCondition` o `END_OF_DATA`. `runBacktest(symbol, bars, riskProfile, conditionId, exitMode)` (Fase 4/6) es ahora un wrapper: `runCombinedBacktest(symbol, bars, riskProfile, conditionId, conditionId, exitMode)`.
- `src/backtestRunner.ts` (`runBacktestForSymbols(pool, symbols, classificationGroup)`, núcleo compartido desde Fase 10) + `src/backtest.ts` (CLI, `npm run backtest` -> `runBacktestForWatchlist(pool)`, wrapper con `symbols=WATCHLIST`, `classificationGroup=null`): para cada símbolo del universo recibido, corre `runCombinedBacktest` sobre las **144 combinaciones** `(buyCondition, sellCondition)` = 12 `CONDITIONS` x 12 `CONDITIONS` (Fase 7, mirror del combo-matrix de `/root/bots/backtests`) con el `settings.exitMode` activo (Fase A.1; `bot_settings`, sin caché), elige el par de mayor `totalReturnPct` entre los que tuvieron >0 trades (fallback `DEFAULT_CONDITION_ID`/`DEFAULT_CONDITION_ID` con `trades: 0` si ninguno operó), persiste esa elección + métricas en `symbol_conditions` (`saveSymbolConditions`, columnas `buy_condition_*`/`sell_condition_*`), agrega métricas de portafolio (nº trades, retorno promedio, win rate promedio, mejor/peor símbolo) SOLO de los pares ganadores, y persiste en `backtest_runs`/`backtest_trades` vía `src/services/backtestStore.ts` (con `classification_group` = `null` para esta corrida legacy). `backtest_runs.params.conditions` registra `{symbol, buyConditionId, sellConditionId}` por símbolo; `backtest_runs.params.exitMode` registra el `exitMode` usado en esa corrida. ⚠️ Si `bot_settings.exit_mode = 'signal_only'` (preset activo desde Fase 7), las 144 simulaciones (y por lo tanto el par ganador por símbolo en `symbol_conditions`) se calculan SIN TP/SL - cambia qué par gana y sus métricas respecto a `'bracket'`. **Fase 8**: este loop NO busca combos de 2-3 condiciones (eso sigue siendo trabajo de `/root/bots/backtests`, no se trae esa búsqueda exhaustiva a vibe-bots) - sigue corriendo igual sobre las 144 combinaciones simples para los 27 símbolos, y su resultado alimenta a `runTradingCycle()`/`/api/trading/status` SOLO para los símbolos SIN entrada en `MULTI_CONDITION_OVERRIDES` (hoy, únicamente `SOXQ`); para los 26 restantes, esta corrida sigue persistiendo en `symbol_conditions` (referencia tier 1, ver `overrideTier` en `/api/conditions`) pero la señal real que opera viene del override. **Fase 10**: `runBacktestForGroup(pool, group)`/`runBacktestForAllGroups(pool)` son los otros wrappers de `runBacktestForSymbols` - mismo cálculo, `symbols` acotado a `getSymbolsByClassification(pool, status)` del grupo (`aptos`/`observados`/`bloqueados`), persistido con ese `classification_group`. Expuesto vía `POST /api/backtest/run?group=...`/`GET /api/backtest/results?group=...` (distintos de `/api/backtesting/*`, que siguen siendo la corrida legacy sin grupo - ambos pares de endpoints coexisten).
- `backtest_runs` (`id, run_at, symbols, start_date, end_date, params JSONB, summary JSONB`) y `backtest_trades` (`id, run_id` FK -> `backtest_runs`, `symbol, entry_date, entry_price, exit_date, exit_price, exit_reason, pnl_pct`).
- `POST /api/backtesting/run` (ejecuta y persiste) / `GET /api/backtesting/results` (última corrida con trades). Integrado en la sección "Resumen por símbolo" del dashboard (botón "Ejecutar backtest", período cubierto, resumen de portafolio y, por símbolo, bloque "Backtest" con trades/win rate/retorno/drawdown).
- `npm run backfill-history` (opcional, no corrido aún): `src/backfillHistory.ts` extiende `market_bars` de ~150 a `BACKFILL_DAYS=1095` velas vía `getDailyBars`+`saveDailyBars` (upsert), para backtests con más historia. No toca `BARS_LOOKBACK_DAYS=220` de la ingesta diaria.

## Capa de IA (Claude, Fase 4/5)

- `ANTHROPIC_API_KEY` (requerida) + `ANTHROPIC_MODEL` (opcional, default `claude-haiku-4-5-20251001`) en `secure/keys.env` - `loadAnthropicConfig()` (`src/config.ts`), lanza si falta la key. **Configurada desde 2026-06-14**: el check `anthropic` (#10) está ✅ y la fase de IA corre en cada ciclo.
- `src/services/claude.ts`: `createAnthropicClient(config)` (axios, `https://api.anthropic.com`, headers `x-api-key` + `anthropic-version: 2023-06-01`). `assessWatchlist(client, model, contexts, macro)` - A LO SUMO una llamada a `/v1/messages` por ciclo, `tool_choice` forzado a `record_assessments`. **Desde Fase 11 (2026-06-21), `contexts` cubre SOLO los símbolos candidatos este ciclo** (`candidateIndexes` en `tradingRunner.ts` = `buyCandidateIndexes` ∪ `sellCandidateIndexes`: señal BUY técnica + no bloqueado + sin posición + sin orden BUY pendiente, o señal SELL técnica + posición abierta) - si no hay ninguno, esta llamada se omite por completo (costo $0, log `(costo $0 este ciclo)`). Antes cubría siempre los 20/27 símbolos del watchlist - ya no. Hasta 2026-06-22 era BUY-only; desde 2026-06-23 también cubre SELL (ver "Fase 11", punto 4). `model` viene de `bot_settings.claude_model` (Fase 5, si no es `NULL`) o de `loadAnthropicConfig().model` (default Haiku 4.5). `contexts` (`SymbolAssessmentContext[]`) incluye `estimatedEntryPrice`/`estimatedExitPrice` algorítmicos (Fase 5) y `buyConditionId`/`buyConditionLabel`/`sellConditionId`/`sellConditionLabel` (Fase 7, mostrados en el prompt como `Condición activa: X` si compra=venta, o `Condición de compra: X | Condición de venta: Y` si difieren). Devuelve `SymbolAssessment[]`: `{symbol, score (-1..1), recommendation: 'buy'|'hold'|'avoid', confidence (0..1), rationale, adjustedEntryPrice, adjustedExitPrice}` (los dos últimos opcionales, Fase 5), más `usage: ClaudeUsage` (tokens + `costUsd` estimado vía `CLAUDE_PRICING`, Fase 11 - ver sección "Fase 11"). `verifyAnthropic(client, model)` = ping mínimo, usado por el check `anthropic` (#10) en `src/diagnostics.ts` (sigue usando `loadAnthropicConfig().model`, no el override de `bot_settings`). `CLAUDE_MODEL_OPTIONS` = lista curada de 3 modelos para el selector del dashboard (Haiku 4.5 / Sonnet 4.6 / Opus 4.8).
- **Gate en `runTradingCycle()`** (`src/tradingRunner.ts`, dos pasadas + fase IA intermedia): la IA **solo puede vetar** una señal BUY o SELL que ya pasó el pre-trade check unificado de `preTradeCheck.ts` (Fase 10 - clasificación/posición/orden pendiente/exposición/máx. posiciones, solo aplica al lado BUY) o el chequeo de posición abierta (lado SELL). Si `assessment.recommendation === 'avoid'` sobre un candidato BUY, la acción es `{ type: 'AI_BLOCKED', symbol, reason: rationale }` en vez de `placeBuyOrder`/`placeBracketBuyOrder` (según `exit_mode`); sobre un candidato SELL (desde 2026-06-23), la acción es `{ type: 'AI_BLOCKED_SELL', symbol, reason: rationale }` en vez de `closePosition` - la posición sigue abierta ese ciclo. `src/trade.ts` imprime ambas con `🤖🚫`. Nunca crea compras/ventas nuevas ni toca señales HOLD. Ver "Fase 11", punto 4, para el detalle completo del lado SELL (filtro de candidatos, semántica de `recommendation` para SELL, alcance del experimento A/B/C/D - que sigue siendo BUY-only).
- **Ajuste de precios (Fase 5)**: ver "Configuración dinámica (Fase 5)" más abajo - `applyPriceAdjustment` (`tradingRunner.ts`) acota `adjustedEntryPrice`/`adjustedExitPrice` a ±10% del valor algorítmico antes de aplicarlos a `signal.estimatedEntryPrice`/`estimatedExitPrice`.
- **Fail-open**: la fase IA está envuelta en try/catch; si `loadAnthropicConfig()` lanza o falla la llamada a Claude, se loguea `Fase de IA (Claude) omitida en este ciclo: ...` y `assessments = new Map()` - el ciclo sigue con el perfil de riesgo/modelo de `bot_settings` y los precios algorítmicos (sin `ai_assessments`, sin `AI_BLOCKED`, sin ajuste de precios).
- `ai_assessments` (tabla independiente, sin FK): `symbol, ts, score, recommendation, confidence, rationale, model, adjusted_entry_price, adjusted_exit_price` (las dos últimas, Fase 5, son las propuestas *crudas* de Claude antes del recorte ±10%). `getLatestAssessments(pool)` (`DISTINCT ON (symbol)`) alimenta `GET /api/assessments`, integrado en la sección "Resumen por símbolo" del dashboard (bloque "Evaluación de IA" por símbolo: score/recomendación/confianza/"Ajuste entrada"/"Ajuste salida"/justificación, refresco cada 60s). El snapshot `trading/<ts>.json` en MinIO ahora incluye `assessments`.

## Configuración dinámica (Fase 5)

- `bot_settings` (tabla singleton, `id=1`, `CHECK (id = 1)`): `risk_preset, position_size_pct, stop_loss_pct, take_profit_pct, max_positions, claude_model, trading_enabled, exit_mode, pending_order_timeout_min, auto_cancel_stale_orders, claude_experiment_enabled, updated_at` (las 3 antes de `updated_at` son de Fase 11, ver sección "Fase 11"). `src/services/settingsStore.ts`: `setupSettingsSchema(pool)` (crea + siembra con los valores "moderado" de `RISK_PROFILE` - 10/3/6/5 -, `claude_model = NULL`, y agrega vía `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`: `trading_enabled BOOLEAN NOT NULL DEFAULT TRUE`, `exit_mode TEXT NOT NULL DEFAULT 'bracket'`, `pending_order_timeout_min INTEGER NOT NULL DEFAULT 60`, `auto_cancel_stale_orders BOOLEAN NOT NULL DEFAULT FALSE`, `claude_experiment_enabled BOOLEAN NOT NULL DEFAULT FALSE`), `getSettings(pool) -> BotSettings` (incluye `tradingEnabled`, `exitMode: 'bracket' | 'signal_only'`, `pendingOrderTimeoutMin`, `autoCancelStaleOrders`, `claudeExperimentEnabled`), `saveSettings(pool, settings)` (incluye `exitMode`, no toca los 4 flags booleanos - cada uno con su propio setter/endpoint), `setTradingEnabled(pool, enabled)` / `setAutoCancelStaleOrders(pool, enabled)` / `setClaudeExperimentEnabled(pool, enabled)`. Leída **sin caché** al inicio de `runTradingCycle()`, `runBacktestForWatchlist()`/`runBacktestForGroup()`, `GET /api/trading/status` y `GET /api/settings`.
- **Interruptor ON/OFF de órdenes a Alpaca** (header del dashboard, `POST /api/settings/trading-enabled` con `{ enabled: boolean }`): en `false`, `tradingRunner.ts` reemplaza la rama BUY/SELL por `{ type: 'TRADING_DISABLED', symbol }` para cualquier señal no-HOLD (bloquea compra y venta), pero pasada 1 (señales) y la fase de IA/`saveSignal`/`saveAssessment` corren igual - el dashboard sigue actualizado. Default `true` (= comportamiento histórico sin cambios).
- `RISK_PROFILE_PRESETS` (`strategy/config.ts`, posición%/SL%/TP%/máx. posiciones): **Conservador** 5/2/4/3, **Moderado** 10/3/6/5 (= default histórico, sin cambios), **Agresivo** 15/5/10/8. "Personalizado" = los 4 campos editados a mano en el dashboard, sin atarse a un preset.
- Selector de modelo Claude: `CLAUDE_MODEL_OPTIONS` (`src/services/claude.ts`) - **lista curada sin texto libre**: `claude-haiku-4-5-20251001` (Haiku 4.5), `claude-sonnet-4-6` (Sonnet 4.6), `claude-opus-4-8` (Opus 4.8). `bot_settings.claude_model = NULL` → usa el default de `loadAnthropicConfig()` (Haiku 4.5); una vez guardado desde el dashboard siempre queda en uno de los 3 IDs curados.
- Límite de ajuste de precios IA: **±10%** del valor algorítmico (`applyPriceAdjustment` en `tradingRunner.ts`). Si Claude propone `adjustedEntryPrice`/`adjustedExitPrice` fuera de ese rango (o no propone nada), se descarta y se usa el valor algorítmico; si ambos ajustes son válidos y `adjustedExit > adjustedEntry`, se aplican a `signal.estimatedEntryPrice`/`estimatedExitPrice` **antes** de `saveSignal` (afecta también la bracket order BUY).
- `GET /api/settings` → `{ ok, settings: BotSettings, presets: RISK_PROFILE_PRESETS, models: CLAUDE_MODEL_OPTIONS }` (`BotSettings.exitMode` viaja en `settings`, no en `presets`/`models`). `POST /api/settings` valida `riskPreset` (∈ `conservador|moderado|agresivo|personalizado`), `riskProfile` (`positionSizePct` ∈ (0,1], `stopLossPct` ∈ (0,1), `takeProfitPct` ∈ (0,2), `maxPositions` entero ∈ [1,20]), `claudeModel` (∈ `CLAUDE_MODEL_OPTIONS` o `null`) y `exitMode` (`VALID_EXIT_MODES = ['bracket', 'signal_only']`, default `'bracket'` si se omite); `400` con mensaje en español si algo no valida, si no `saveSettings` + `{ ok: true, savedAt }`.
- **Fase A.1 (2026-06-15) - "Sin bracket (solo señal)" (`exit_mode = 'signal_only'`)**: 4º modo de salida, alternativa a los 3 presets de riesgo (que siempre usan `exit_mode = 'bracket'`). En `tradingRunner.ts`, la rama BUY bifurca sobre `settings.exitMode`: con `'signal_only'` llama a `placeBuyOrder` (`services/alpaca.ts`, orden límite simple `order_class` por defecto, sin `take_profit`/`stop_loss`) y guarda el registro en `trading_orders` con `orderType: 'simple'` y `takeProfitPrice`/`stopLossPrice` `NULL`; con `'bracket'` (default, sin cambios) sigue llamando a `placeBracketBuyOrder` con `orderType: 'bracket'`. `TradingAction` (`OPEN_POSITION`) tiene ahora `takeProfitPrice: number | null` / `stopLossPrice: number | null`; `trade.ts#describeAction` imprime "sin bracket TP/SL" cuando son `null`. La salida en `signal_only` es **únicamente** por señal SELL de la condición activa - `closePosition()` en la rama SELL de `runTradingCycle()` no cambió (ya cerraba cualquier posición abierta, bracket o no). `positionSizePct`/`maxPositions` del perfil de riesgo activo siguen aplicando igual; `stopLossPct`/`takeProfitPct` se guardan pero no se usan en este modo. `strategy/backtest.ts#runBacktest` también respeta `exitMode` (mismo branch, sin TP/SL) - ver "Backtesting (Fase 4/6)" para el impacto en `symbol_conditions`.
- Dashboard: sección "Configuración" (entre "Ingesta de datos" y "Resumen por símbolo"), con preset de riesgo + 4 campos numéricos editables (editar cualquiera cambia el preset a "Personalizado") y selector de modelo Claude. El selector `#risk-preset` tiene una 5ª opción "Sin bracket (solo señal)" (`value="sin_bracket"`, solo en el frontend) que mapea a `exitMode: 'signal_only'` + `riskPreset: 'personalizado'` al guardar (`app.js#saveSettings`); al cargarla deshabilita (no oculta) los inputs de TP/SL (`updateRiskInputsForPreset`), que siguen viajando en `riskProfile` con su último valor válido. Cambios aplican desde el próximo ciclo de trading/backtest, sin reiniciar el dashboard ni el bot (nunca systemd, regla 1). El interruptor ON/OFF de órdenes a Alpaca vive aparte, en el header (aplica de inmediato, no requiere "Guardar").
- ⚠️ Cambia comportamiento de trading real (regla 2): el perfil de riesgo activo, el modo de salida (`exit_mode`), el modelo de IA y los precios de entrada/TP de las bracket orders BUY ya no son constantes de compilación - se leen de `bot_settings` en cada ciclo.

## Fase híbrido (2026-06-15, branch `hibrido` - ⚠️ código presente en `main` pero ORQUESTACIÓN INACTIVA, ver corrección al inicio de este archivo)

Diseño original (lo que describe esta sección de acá en adelante) basado en `phase1_full20` de `bots/backtests`: 1D sigue siendo la columna vertebral de los 20 símbolos originales; solo 5 tendrían un cambio puntual, en 3 niveles (`strategy/hybridConfig.ts#HYBRID_CONFIG`). **Repetimos la advertencia de arriba porque es fácil pasarla por alto**: nada de la orquestación de Pasada 1/1b descrita abajo corre hoy en `runTradingCycle()` - es la documentación del diseño tal como se construyó, no una descripción del comportamiento actual del bot. Lo único realmente activo es el pick informativo 1H de `backtestRunner.ts` (ver "Mapa del repo").

- **Tier 1 (in-place)** - `SPY` (compra `bollinger_reversion` / venta `sma_cross_10_30`), `XLU` (compra `bollinger_reversion` / venta `macd_cross`): la señal "main" de ese símbolo se calcula con su combo 1H (`computeSignal1H`) en vez del pick 1D de `symbol_conditions`. Reemplaza, no agrega - persiste en `trading_signals` con `system='main', timeframe='1Hour'`.
- **Tier 2 (paralelo)** - `MS`, `QQQM` (ambos: compra `stochastic_cross` / venta `ema_cross_12_26`): además del flujo 1D normal sin cambios, se evalúa el combo 1H con `PARALLEL_RISK_PROFILE` propio (5% equity/posición, sin TP/SL, máx. 2 posiciones paralelas en total) y posiciones trackeadas en `parallel_positions` (`services/parallelStore.ts`), independientes de la posición 1D del mismo símbolo. Persiste con `system='parallel', timeframe='1Hour'`.
- **Tier 'shadow'** - `SCHD` (compra `cci_reversal` / venta `sma_cross_20_50`): igual que Tier 2 pero solo registra la señal (`system='shadow', timeframe='1Hour'`) - nunca coloca órdenes ni toca `parallel_positions`. Pensado para acumular trades antes de promover a Tier 2.
- Los 13 símbolos restantes (11 NEUTRO + `AAPL` + `SCHF`, no listados en `HYBRID_CONFIG`) siguen 100% en el pick 1D de `symbol_conditions`, sin cambios.

**Velas 1H** (`conditions1h.ts`): `SCALE_1H=8` reescala todos los períodos de indicador (SMA10->80, RSI14->112, etc.) para preservar el significado "calendario" de cada condición sobre velas horarias; `MIN_BARS_1H = 401`. `CONDITIONS`/`evaluate`/`describe` (`conditions.ts`) no cambian - leen los mismos campos del `IndicatorContext`, ahora poblados con períodos x8.

**Ingesta 1H** (`tradingRunner.ts`, dentro de `runTradingCycle()`, best-effort try/catch): `getHourlyBars(marketDataClient, HYBRID_SYMBOLS, HOURLY_BARS_LOOKBACK_DAYS=200)` -> `saveHourlyBars(pool, bars)`, reusa `market_bars` con `timeframe='1Hour'` (misma PK `symbol+timeframe+ts`). `HOURLY_BARS_FOR_SIGNAL=450` velas se piden a `getRecentOhlcBars1H` para `computeSignal1H`. ⚠️ **Gotcha confirmado**: `getHourlyBars`/`getDailyBars` (`/v2/stocks/bars`) viven en la MARKET DATA API (`https://data.alpaca.markets`, cliente de `createMarketDataClient(alpacaConfig)`), NO en la TRADING API (`createAlpacaClient`, `paper-api.alpaca.markets`) - usar el cliente equivocado da 404. Verificado end-to-end 2026-06-15 (smoke test, luego eliminado): 5196 velas 1H descargadas (200 días, 5 símbolos) y guardadas correctamente.

**Orquestación en `runTradingCycle()`** (`tradingRunner.ts`), dos pasadas + 2 sub-pasadas:
- Pasada 1 (señales "main"): por símbolo, si `HYBRID_CONFIG[symbol]?.tier === 1` usa `computeSignal1H` + combo 1H fijo del `HYBRID_CONFIG`; si no, el flujo 1D habitual (`symbol_conditions`).
- Pasada 1b (nueva): para `[...TIER2_SYMBOLS, ...SHADOW_SYMBOLS]` (MS, QQQM, SCHD), `computeSignal1H` con `PARALLEL_RISK_PROFILE` y el combo 1H del `HYBRID_CONFIG` -> `hybridSignals: HybridSignalResult[]` (`{symbol, system: 'parallel'|'shadow', signal}`). NO pasa por la fase de IA (Claude) - esa fase sigue evaluando solo `signals` (Pasada 1).
- Pasada 2 (sin cambios de lógica): `saveSignal(pool, signal, 'main', timeframe)` - `timeframe='1Hour'` solo para Tier 1, `'1Day'` para el resto.
- Pasada 2b (nueva): para cada `hybridSignals`, `saveSignal(pool, signal, system, '1Hour')`. `system='shadow'` -> solo `NO_ACTION` (logging). `system='parallel'` -> respeta el interruptor ON/OFF (`TRADING_DISABLED`); BUY abre con `placeBuyOrder` + `openParallelPosition` (qty = `floor(equity * 0.05 / price)`, `OPEN_PARALLEL_POSITION`) si no hay posición paralela abierta para ese símbolo y `openParallelCount < PARALLEL_RISK_PROFILE.maxPositions=2`; SELL cierra con `closePositionQty(alpacaClient, symbol, openParallel.qty)` + `closeParallelPosition` (`CLOSE_PARALLEL_POSITION`) - vende SOLO la porción paralela, sin tocar la posición "main" del mismo símbolo en Alpaca.

**`TradingAction`** (`tradingRunner.ts`) agrega `OPEN_PARALLEL_POSITION`/`CLOSE_PARALLEL_POSITION`, y `TRADING_DISABLED`/`NO_ACTION`/`SKIPPED`/`ERROR` ahora llevan `system?: 'main'|'parallel'|'shadow'`. `trade.ts#describeAction` los imprime con sufijo `(paralelo)`/`(sombra)`/`(${system})`; `trade.ts#main` imprime `hybridSignals` aparte ("Señales híbridas (1H, paralelo/sombra)").

**`/api/trading/status`** (`server.ts`): `signals` (los 20 símbolos) usa la misma rama Tier1 (`computeSignal1H`) que `tradingRunner.ts` para que el dashboard muestre la señal real que decide la posición; nuevo array `hybridSignals` (Tier2+shadow, igual cálculo que la Pasada 1b + overlay de `getLatestHybridSignals`) y `parallelPositions` (`getRecentParallelPositions(pool, 20)`) en la respuesta JSON.

**Dashboard** (`public/index.html`/`app.js`): nueva sección "Sistema híbrido (Tier 2 paralelo / sombra)" antes de "Snapshots (MinIO)", con tabla `#hybrid-signals-table` (señales 1H de MS/QQQM/SCHD: símbolo, sistema Paralelo/Sombra, señal, precio, condiciones, motivo) y `#parallel-positions-table` (posiciones Tier 2: símbolo, cantidad, precio entrada, estado, abierta/cerrada, precio salida) vía `renderHybridSignalsTable`/`renderParallelPositionsTable`.

⚠️ **Estado real verificado 2026-06-21** (reemplaza la nota histórica de "pendiente antes de mergear", ya no aplica tal como estaba escrita):
- El código y la migración de DB (`system`/`timeframe` en `trading_signals`/`trading_orders`) SÍ están en `main` y aplicados en producción (la migración corre en cada `runTradingCycle()`, que viene corriendo por cron hace tiempo) - ver "DB - columnas relevantes" arriba.
- Lo que NO pasó fue activar la orquestación (Pasada 1/1b en `tradingRunner.ts`) - quedó sin cablear, posiblemente a propósito al priorizar Fase 8 (multi-condición 1D) como la vía elegida en su lugar. No hay evidencia de una decisión explícita de "descartar el híbrido" - solo de que no se completó su integración.
- **`PARALLEL_RISK_PROFILE`** (5% equity/posición, `signal_only`, máx. 2 posiciones paralelas) sigue siendo un default nunca confirmado explícitamente por el usuario, por si se retoma esta fase.
- Si se quiere reactivar: además de re-cablear la orquestación, recordar que el filtro de candidatos de la fase de IA (Fase 11, BUY+SELL desde 2026-06-23) y el pre-trade check unificado (Fase 10) no fueron diseñados pensando en `system='parallel'`/`'shadow'` - revisar interacción antes de activar.

## Fase 8 (2026-06-17, branch `multi-condicion-tiers` - **mergeada a `main`**, commit `f74dc9f`)

Basada en el reporte de 3 niveles de `/root/bots/backtests` (`output/report_data.json`/`report.html`, 33 símbolos, generado 2026-06-17): 1 condición (144 combos, = lo que ya hace `npm run backtest`/Fase 7), 2 condiciones (17.424 combos, AND/OR) y 3 condiciones (3.097.600 combos, 8 formas lógicas), todo bajo `exit_mode='signal_only'`. El reporte recomienda, por símbolo, el tier MÁS SIMPLE que mejora de forma real con muestra confiable (`MIN_TRADES=5`, `MIN_BARS=300`). **Independiente de "Fase híbrido"** (branch `hibrido`, arquitectura 1H) - no relacionadas, no mezclar.

**Expresiones de condición** (`strategy/conditionExpr.ts`): generaliza `buyConditionId`/`sellConditionId` (antes, Fase 7, siempre un id de `CONDITIONS`) para aceptar combinaciones booleanas de 2-3 condiciones - ver entrada en "Mapa del repo" más arriba para la gramática soportada. Un id simple sigue siendo una expresión trivial válida, así que todo el código de Fase 7 (`symbol_conditions`, `DEFAULT_CONDITION_ID`, picks 1H de `hybridConfig.ts`) sigue funcionando sin cambios - cero regresión. `signals.ts#computeSignalWith` y `strategy/backtest.ts#runCombinedBacktestWith` ahora resuelven `buyConditionId`/`sellConditionId` vía `parseConditionExpr`/`evaluateConditionExpr`/`describeConditionExpr` en vez de `CONDITIONS.find(...)` directo.

**Overrides estáticos por símbolo** (`strategy/multiConditionOverrides.ts#MULTI_CONDITION_OVERRIDES`, mismo patrón de precedencia que `hybridConfig.ts#HYBRID_CONFIG` para tier 1): 26 símbolos de los 27 del watchlist (todos menos `SOXQ`) tienen una expresión de compra/venta de 2 o 3 condiciones que reemplaza al pick de 1 condición de `symbol_conditions` en `tradingRunner.ts` (Pasada 1) y `server.ts` (`/api/trading/status`, `/api/conditions`) - se consulta ANTES de `symbol_conditions`/`DEFAULT_CONDITION_ID`. `SOXQ` queda sin entrada porque el reporte concluyó que ni 2 ni 3 condiciones mejoran de forma robusta sobre su condición simple actual - sigue 100% en el flujo de Fase 7.

**Ampliación del watchlist** (20 -> 27 símbolos, ver sección "Watchlist" más arriba): se reincorporaron `AVGO, HD, LOW, MAIN, MU, SCHG, SHW` (señal "usar" en el reporte + volumen diario líquido, decenas/cientos de miles de acciones/día verificado contra `market_bars` el 2026-06-17). Símbolos del reporte que NO se reincorporaron:
- **`QQQ`** - exclusión **permanente**, no reabrir. Redundante con `QQQM` (mismo índice Nasdaq-100, mismas empresas/pesos, comisión mayor 0.20% vs 0.15%) - mismo motivo que la exclusión de 2026-06-14.
- **`GOLD`** - bloqueado. Verificado contra Alpaca (`GET /v2/assets/GOLD`) el 2026-06-17: el ticker resuelve hoy a **"Gold.com, Inc."**, NO a Barrick Gold - el histórico backtested (que dio el resultado "usar" del reporte) casi seguro corresponde a una empresa completamente distinta a la que es tradable hoy bajo ese símbolo. No se agregó al watchlist; no reconsiderar sin una verificación de identidad de instrumento nueva.
- **`AGM`, `DBEZ`, `NECB`, `PPA`** - excluidos por volumen diario muy bajo (198-2.542 acciones/día contra `market_bars`, verificado 2026-06-17, vs. decenas/cientos de miles del resto del watchlist) - riesgo de slippage/fills pobres en paper que el backtest no mide. Mismo motivo de iliquidez que los excluyó originalmente el 2026-06-14; este reporte no lo re-evalúa (solo mide retorno/trades, no liquidez).
- **`SCHG` SÍ se reincorporó** a pesar de la tensión sin resolver: su exclusión original (2026-06-14) fue por ser casi duplicado de `VUG` (r=0.993, comisión mayor 0.04% vs 0.03%) - este reporte no vuelve a chequear esa correlación, así que el hallazgo "usar con 3 condiciones" no la invalida ni la confirma. Decisión explícita del usuario: agregarlo igual (2026-06-17).
- **Correlaciones informativas** (no bloquean código, son nota de portfolio): `AVGO`/`MU` son top holdings de `SOXQ`/`SPMO` (sector semiconductores) - si el bot ya opera esos ETFs, parte de la señal de `AVGO`/`MU` ya está implícita en ellos.

**`/api/conditions`** (Fase 8, ver detalle en su sección más arriba): agrega `overrideTier: 2 | 3 | null` y, si hay override, sobrescribe `buyConditionId`/`buyConditionLabel`/`sellConditionId`/`sellConditionLabel` con la expresión activa real (vía `labelConditionExpr`) - las métricas (`trades`/`winRatePct`/`totalReturnPct`/`maxDrawdownPct`) siguen siendo las del tier 1 (`symbol_conditions`), no se recalculan para el override (queda como posible mejora futura, no bloqueante).

**Dashboard** (`public/app.js`): `mergeConditionChartConfig`/`condShort` generalizados para extraer y combinar TODOS los ids hoja de una expresión compuesta (antes asumían un único id por lado) - si un id no está en el catálogo de overlays/abreviaturas, cae a "sin overlay"/al id crudo sin romper.

**Verificación realizada 2026-06-17** (antes de commitear): `npm run build` limpio; self-check de `MULTI_CONDITION_OVERRIDES` (26/26 expresiones parsean sin error, confirmado también que un id inventado y un paréntesis sin cerrar lanzan error en vez de degradar en silencio); `runCombinedBacktest` con cada override contra el `market_bars` ACTUAL de vibe-bots (no el snapshot que usó `bots/backtests`) - los 26 símbolos dieron resultados no-degenerados (trades > 0, sin NaN), con magnitudes distintas a las del reporte (esperado, por diferencia de fecha de corte entre snapshots) pero mismo signo/orden de magnitud; `npm run backtest` corrido sobre los 27 símbolos (sembró el baseline tier 1 para los 7 nuevos); dashboard levantado (`web:start`) y verificado vía curl - `/api/trading/status`/`/api/conditions` devuelven labels compuestas legibles para los símbolos con override y el pick sin cambios de `SOXQ`; `npm run ingest` corrido sobre los 27 símbolos sin errores (4.050 barras = 150 x 27).

✅ **Mergeada y desplegada** (confirmado 2026-06-21 vía `git log`/`git merge-base`). Pendiente real, no bloqueante: las métricas de `/api/conditions` para símbolos con override no están recalculadas con la expresión activa (ver bullet de arriba) - posible mejora futura.

## Fase 9 (2026-06-18, branch `ui-refactor-classification` - **mergeada a `main`**, commit `a6052a0`)

Dos partes independientes pero entregadas juntas: clasificación manual por símbolo con hard-block real, y un rediseño del dashboard a tabs (reemplaza el scroll infinito de una sola página).

**Clasificación manual** (`services/symbolClassificationStore.ts`): tabla nueva `symbol_classifications (symbol TEXT PRIMARY KEY, status TEXT CHECK IN ('apto','observar','bloqueado'), updated_at TIMESTAMPTZ, updated_by TEXT NULL)`, creada por `setupSymbolClassificationSchema(pool)` (llamada desde `runTradingCycle()` y desde las dos rutas nuevas de `server.ts`, mismo patrón que `bot_settings`/`symbol_conditions`). Se siembra UNA sola vez (solo si la tabla está vacía, para no pisar clasificaciones manuales ya guardadas) con defaults fijados el 2026-06-18 sobre el watchlist de 27 símbolos: `bloqueado` = `SOXQ, AMZN, TSM, SCHG, SPMO, HD, MSFT, XMMO`; `observar` = `QQQM, MAIN, SPY, VUG`; el resto (15 símbolos) `apto`. `getAllSymbolClassifications`/`getSymbolClassification`/`setSymbolClassification` usan una caché en memoria a nivel de módulo con TTL 30s (`Map` + `expiresAt`), invalidada inmediatamente en cada `set` - en `tradingRunner.ts` (proceso corto por ciclo, vía `ts-node` desde cron) esto ahorra ida y vuelta a la DB por símbolo dentro del mismo ciclo; en `server.ts` (proceso largo, dashboard) sirve de caché real entre polls.

**API** (`server.ts`): `GET /api/symbol-classifications` → `{symbol: status, ...}` (mapa plano, no envuelto en `{ok, ...}` - distinto al resto de los endpoints, a propósito). `POST /api/symbol-classifications/:symbol` con body `{status}` → valida `symbol` contra `WATCHLIST` y `status` contra `SYMBOL_CLASSIFICATION_STATUSES` (400 con mensaje en español si no), si no `{ok: true, symbol, status, updatedAt}`.

**Hard-block** (`tradingRunner.ts`, dentro de la rama `signal.signal === 'BUY'` de la Pasada 2, ANTES del check de posición/orden pendiente/máx. posiciones): si `getSymbolClassification(pool, symbol) === 'bloqueado'`, la acción es `{type: 'NO_ACTION', symbol, reason: 'BLOQUEADO_MANUAL'}` (logueada con `console.log`) en vez de evaluar la compra - no llega a tocar `placeBuyOrder`. No afecta SELL/HOLD ni el resto de la lógica (gating de IA, ajuste de precios, etc.), que sigue corriendo igual antes de este check.

**Verificación 2026-06-18**: `npm run build` limpio; `curl` contra una instancia temporal (puerto 4099, sin tocar el dashboard real) confirmó GET/POST funcionando, incluyendo los 400 de validación; el día de la verificación las 27 señales recalculadas dieron `HOLD` (sin ningún `BUY` real para forzar el camino completo en `runTradingCycle()`), así que la rama `BLOQUEADO_MANUAL` se validó con un arnés que ejercita el store real (DB real, mismo `getSymbolClassification`) contra una señal `BUY` sintética, confirmando que produce `{type: 'NO_ACTION', reason: 'BLOQUEADO_MANUAL'}` - reproduce literal el bloque agregado en `tradingRunner.ts`. Adicionalmente se corrió `npm run trade` una vez completo (real, paper) para confirmar que el ciclo entero no se rompe con el schema/check nuevos. ⚠️ Pendiente: re-confirmar en los logs de producción (`logs/trade-cron.log`) la próxima vez que un símbolo `bloqueado` tenga una señal `BUY` real - no observado todavía end-to-end con datos de mercado reales.

**Dashboard con tabs** (`public/index.html`/`app.js`/`styles.css`, jerarquía inspirada en `bots/analyst/frontend` - sin copiar su código): reemplaza la página única de scroll infinito por un layout con sidebar fija (`Servicios` resumido, `Próximo ciclo` - texto estático de la cadencia de cron, `Cuenta Alpaca`, `Posiciones abiertas` con conteo) + 5 tabs en el área principal:
- **Resumen** (default): una fila compacta por símbolo del watchlist (27, ya no 20+héroes-1H) con columnas `Símbolo | Tipo | Estado | Bloqueo | Señal | IA | P. entrada | Condición`. `Estado` es un `<select>` editable (✅ Apto / 🟡 Observar / ❌ Bloqueado) que hace `POST /api/symbol-classifications/:symbol` al cambiar y actualiza la fila sin recargar; `Bloqueo` es un badge derivado (🚫 solo si `status==='bloqueado'`). Fila coloreada según estado (verde/amarillo/rojo muy sutil, `tr.row-apto`/`row-observar`/`row-bloqueado`). Filtros encima de la tabla (Estado/Tipo/Señal/búsqueda por símbolo, client-side, sin refetch) con un link "Limpiar filtros" en el empty state cuando los filtros no matchean nada.
- **Detalle**: vacío hasta hacer click en una fila de Resumen (`openDetail(symbol)`) - entonces renderiza UNA card grande (gráfico 420px, stats completos, motivo, posición, evaluación de IA, backtest, dropdown de clasificación también editable ahí) para ese símbolo puntual. Reemplaza el grid viejo que renderizaba las 27 cards (con sus 27 gráficos Chart.js) siempre, en cada carga - mejora de performance además de UX.
- Checkboxes "Medias móviles" / "Bollinger" / "Oscilador" sobre el gráfico de Detalle (`state.chartToggles`, persiste mientras dure la sesión del navegador): togglean qué overlays de `CONDITION_CHART_CONFIG` se dibujan - "Bollinger" filtra por `key` que empieza con `bb`, "Medias móviles" el resto de los overlays de precio (SMA/EMA/canal Donchian), "Oscilador" el panel secundario (`y1`) completo.
- **Backtest**: la tabla `#conditions-table` de antes, sin cambios de lógica en esta fase (Fase 10 le agrega un selector de grupo encima).
- **Operaciones**: `#positions-table` + `#orders-table` + el botón "Ejecutar ciclo de trading", sin cambios de lógica en esta fase (Fase 10 agrega `#pending-orders-table`, pills de cuenta y botón de sync; Fase 11 agrega el toggle de limpieza de órdenes huérfanas).
- **Sistema**: health-grid + ingesta + configuración + snapshots, sin cambios de lógica en esta fase (Fase 11 agrega la sección "Experimentos").

Verificado 2026-06-18 con Playwright (Chromium headless, sin instalar nada nuevo - se reusó un binario ya cacheado en `/root/.cache/ms-playwright`) contra el dashboard real corriendo en `:4000`: las 5 tabs renderizan sin errores de consola, el dropdown de clasificación actualiza el badge sin recargar, el click en fila abre Detalle con gráfico y datos, los filtros narrowean la tabla, y los toggles de overlay esconden/muestran Bollinger/oscilador correctamente.

✅ **Mergeada y desplegada** (confirmado 2026-06-21). Nota histórica sin impacto actual: durante la verificación original se mató por error el proceso del dashboard real (PID equivocado al limpiar un proceso de prueba en otro puerto) - se reinició de inmediato con `./scripts/start-web.sh`, downtime de menos de un minuto, sin pérdida de datos.

## Fase 10 (operaciones multi-cuenta de solo lectura + backtests segmentados - **mergeada a `main`**)

Sin branch propia identificada en `git log` (llegó vía varios merges: `operations-multi-account`, `backtest-by-classification`, `backtest-classification-filter-fix`) - "Fase 10" es el número que el propio código usa en un comentario de `backtestRunner.ts` para `runBacktestForGroup`, reusado acá para tener un nombre estable.

**Backtests segmentados** (`backtestRunner.ts`): `runBacktestForSymbols(pool, symbols, classificationGroup)` es ahora el núcleo compartido (144 combos, igual que siempre) - `runBacktestForWatchlist(pool)` (CLI/`npm run backtest`, legacy, `classificationGroup=null`) y `runBacktestForGroup(pool, group)`/`runBacktestForAllGroups(pool)` (nuevo, `symbols` acotado a `getSymbolsByClassification(pool, status)` del grupo `aptos`/`observados`/`bloqueados`) son wrappers - mismo cálculo, distinto universo de símbolos, persistido con `backtest_runs.classification_group` (`null` para la legacy). Endpoints nuevos `POST /api/backtest/run?group=...`/`GET /api/backtest/results?group=...`, que **coexisten** con los legacy `/api/backtesting/run|results` (sin grupo) - no se unificaron.

**Operaciones multi-cuenta - leer con cuidado, es más limitado de lo que suena**:
- `services/alpaca.ts`: `AccountGroup = 'aptos' | 'observados' | 'bloqueados'`, `getAlpacaClient(group)` lee credenciales `ALPACA_<GRUPO>_KEY/_SECRET/_ENDPOINT` de env y devuelve `null` con un warning si faltan. Hoy están configuradas `ALPACA_APTOS_*` y `ALPACA_OBSERVADOS_*`; `ALPACA_BLOQUEADOS_*` **no está configurada**.
- `services/operationsSync.ts`: `setupOperationsSyncSchema` crea `account_state` (PK `account_group`: equity/cash/buying_power/etc.), `positions_snapshot`/`pending_orders_snapshot`/`executed_orders_snapshot` (PK `account_group`+símbolo/order id). Un poller en background (arrancado desde `server.ts`) sincroniza periódicamente cada grupo CONFIGURADO contra Alpaca real (`getAccount`/`getPositions`/`getOpenOrders`/`getClosedOrders` con el cliente de ese grupo) y persiste el snapshot - es la única fuente de los datos multi-cuenta, no se recalculan al vuelo en cada request.
- `server.ts`: `GET /api/operations?account=<grupo|all>` (lee los snapshots), `GET /api/account-status?account=...` (solo `account_state`), `POST /api/operations/sync?account=...` (fuerza un sync). Consumido por el tab "Operaciones" (pills "Todas/✅ Aptos/🟡 Observados/❌ Bloqueados").
- ⚠️ **Lo que NO hace**: `runTradingCycle()` sigue colocando/cerrando **todas** las órdenes reales con el cliente único de `ALPACA_API_KEY/SECRET/BASE_URL` (`createAlpacaClient(loadAlpacaConfig())`, ver `tradingRunner.ts` línea donde se inicializa `alpacaClient` al principio de la función) - `account_group` (derivado de `symbol_classifications` vía `classificationToAccountGroup()`) **solo etiqueta** `trading_signals.account_group`/`trading_orders.account_group` para que el tab Operaciones filtre coherentemente. No hay ruteo real hacia la cuenta de Alpaca de ese grupo. No asumir que "aptos"/"observados" son cuentas que efectivamente operan - hoy son solo vistas de lectura.

**Pre-trade check unificado** (`services/preTradeCheck.ts`, motivado por un bug real de duplicación de BUYs): `canPlaceBuyOrder(pool, symbol, group, ctx)` reemplaza los checks sueltos que tenía `tradingRunner.ts` - evalúa en orden fail-fast: clasificación `bloqueado` (`SYMBOL_BLOCKED_MANUAL` - mismo hard-block de Fase 9, ahora centralizado acá) → posición abierta (`POSITION_ALREADY_OPEN`) → orden BUY pendiente (`PENDING_BUY_ORDER`, **filtra por `side==='buy'`** - el bug era que una SELL pendiente bloqueaba un BUY nuevo sin motivo) → tope de exposición por símbolo (`EXPOSURE_LIMIT_EXCEEDED`, `equity * riskProfile.positionSizePct` contra posición+pendientes+la orden nueva) → máximo de posiciones (`MAX_POSITIONS_REACHED`). Cachea el resultado 30s por `group+symbol` (`invalidateBuyCheck` limpia al colocar/cancelar o al final del ciclo) - `getSymbolClassification` adentro ya tiene su propia caché de 30s, así que esto es una segunda capa para no repetir el resto del contexto (posición/órdenes/equity) dentro del mismo ciclo.

## Fase 11 (eficiencia y experimento de sesgo de Claude, 2026-06-21 - **mergeada a `main`**)

Originalmente tres piezas (2026-06-21); se agregó una 4ª el 2026-06-23 (veto de IA extendido a SELL). Todas en `tradingRunner.ts`/`services/claude*.ts`/`services/staleOrders.ts`:

**1) Filtro de candidatos para la IA** (`tradingRunner.ts`, antes de la fase de evaluación): se construyen `buyCandidateIndexes` (símbolos con `signals[i].signal === 'BUY'`, clasificación distinta de `'bloqueado'`, sin posición abierta y sin orden BUY pendiente) y `sellCandidateIndexes` (símbolos con `signals[i].signal === 'SELL'` y posición abierta - sin esa posición, cerrar sería un no-op y evaluarlo sería gastar tokens al pedo) - `assessWatchlist()` (la llamada de producción) se llama con la unión de ambos (`candidateIndexes`), y se **omite por completo** (costo $0, log explícito) si no hay ningún candidato. Originalmente (2026-06-21) este filtro era BUY-only y además evaluaba siempre los 20/27 símbolos del watchlist en cada ciclo, **con un gate de "como mínimo 1 vez por día" vía una clave de Redis `claude:last_run_date`** (comentario explícito en el código, `tradingRunner.ts`) - ese gate de una-vez-por-día se ELIMINÓ junto con el filtro BUY-only original, no se reemplazó por nada equivalente. Combinado con que el cron de trading real corre cada 5 min (ver "Automatización"), un candidato persistente (señal técnica que no cambia entre corridas porque viene de barras diarias) puede ser re-evaluado por Claude muchas veces el mismo día - ver el aviso de costo en "Automatización". ⚠️ **Ampliado a SELL con veto real 2026-06-23** (decisión explícita del usuario, ver bullet nuevo más abajo) - el filtro de exclusión de BUY (posición abierta/orden pendiente) se agregó el mismo día, motivado por un caso real: SCHD generaba una evaluación de Claude cada 5 min con una posición ya comprada, sin que el veredicto pudiera cambiar nada (`preTradeCheck.ts#POSITION_ALREADY_OPEN` bloquea incondicionalmente, después de la IA).

**2) Experimento de sesgo A/B/C/D** (opcional, flag `bot_settings.claude_experiment_enabled`, default `false`): para cada candidato BUY (mismo filtro de arriba), 3 llamadas extra a Claude con prompts distintos (`buildVariantPrompt` en `claude.ts`, `ClaudeExperimentVariant = 'B'|'C'|'D'`):
- **B** (sin señal técnica): mismo contexto que producción MENOS la condición técnica activa y los precios estimados algorítmicos.
- **C** (solo señal técnica): SOLO la condición técnica + precio, sin fundamentales/noticias/macro.
- **D** (orden invertido): igual contenido completo que la variante de producción ("A"), con las secciones en orden inverso (detecta sesgo de anclaje).
- **A** no es una llamada extra - es la evaluación de producción ya obtenida (punto 1), reusada tal cual y registrada igual en `claude_gate_experiment` para poder comparar.
- Persistencia: `claude_gate_experiment` (`services/claudeExperimentStore.ts`, `symbol, ts, variant, recommendation, score, confidence, rationale, model, tokens_used, cost_estimate_usd` - las 4 variantes de un símbolo en un ciclo comparten el mismo `ts`, capturado una vez por el caller, para poder hacer self-join sin depender de ventanas de tiempo aproximadas). Endpoints: `GET /api/claude-experiment/summary|disagreements|cost|latest?days=N`. Toggle: `POST /api/settings/claude-experiment-enabled`. Dashboard: sección "Experimentos" (tab Sistema) + dropdown de variante por símbolo en el tab Resumen (`state.experimentVariantSelection` en `app.js`).

**3) Visibilidad de costo** (siempre activa, sin flag): cada llamada real a `/v1/messages` (producción o experimento) registra tokens + costo estimado en `claude_usage_log` (`services/claudeUsageStore.ts`, upsert diario UTC: `date, total_tokens, total_cost_usd, calls_count, calls_production, calls_experiment`). El costo usa `CLAUDE_PRICING` (`claude.ts`, USD/millón de tokens hardcodeado con precios oficiales: Haiku 4.5 `1.0/5.0`, Sonnet 4.6 `3.0/15.0`, Opus 4.8 `5.0/25.0`) sobre `usage.input_tokens`/`usage.output_tokens` reales de la respuesta - nunca inventado. `GET /api/claude-usage?days=1` alimenta el banner del header ("Claude hoy: $X · N llamadas"). ⚠️ **Regla explícita de la tarea que lo introdujo, preservada en un comentario de `claudeUsageStore.ts`**: "NO implementes ningún corte automático de consultas a Claude por presupuesto" - el control de gasto es manual (apagar el experimento o revocar `ANTHROPIC_API_KEY`), nunca un cutoff en código.

**4) Veto de IA extendido a SELL** (2026-06-23, decisión explícita del usuario - antes la IA NUNCA evaluaba señales SELL, sin importar si había o no posición abierta en Alpaca; lo confirmó leyendo `tradingRunner.ts` el mismo día tras notar que XLP tenía señal SELL sin evaluación de Claude). `sellCandidateIndexes` (ver punto 1) entra al mismo `assessWatchlist()` de producción que los candidatos BUY - **no** al experimento A/B/C/D (`runClaudeExperiment` sigue filtrando a `signal==='BUY'` explícitamente en el call site, para no multiplicar el costo sin que se haya pedido). En la Pasada 2, la rama SELL ahora chequea `assessment?.recommendation === 'avoid'` ANTES de cerrar la posición (mismo lugar donde antes solo se chequeaba `!position`): si vetó, la acción es `{ type: 'AI_BLOCKED_SELL', symbol, reason: rationale }` (`trade.ts` la imprime con `🤖🚫`) y la posición sigue abierta ese ciclo - sin caché de "ya vetado", así que si la señal técnica sigue SELL el próximo ciclo, se vuelve a evaluar (mismo patrón sin throttle que ya tiene el lado BUY, ver el aviso de costo arriba). El prompt (`claude.ts#buildPrompt`) tuvo que aclarar explícitamente la semántica de `recommendation` por tipo de señal, porque el campo `"hold"` es ambiguo en contexto SELL: para un símbolo con "Señal técnica: SELL", `"hold"` significa "no objeto, procede con el cierre" (NO "mantener la posición" - el nombre del campo es por consistencia con el esquema, no describe el efecto) y `"avoid"` significa "vetá el cierre, la posición sigue abierta". El esquema de la tool (`RECORD_ASSESSMENTS_TOOL`, enum `'buy'|'hold'|'avoid'`) no cambió - alcanza con la reinterpretación contextual, y `parseAssessments` ya degradaba cualquier valor que no sea literalmente `'buy'`/`'avoid'` a `'hold'`, que sigue siendo el fallback seguro (deja proceder la señal técnica sin vetar). El email de la orden ejecutada (Fase 12, `TradeAlertEntry.ai`) ahora también se llena para SELL cuando hubo evaluación (antes siempre `null` con el comentario "Fase 11 solo evalúa BUY", ya no aplica).

**Limpieza automática de órdenes BUY huérfanas** (opcional, flag `bot_settings.auto_cancel_stale_orders`, default `false`; timeout `bot_settings.pending_order_timeout_min`, default `60`): `findStaleOrders()`/`cancelStaleOrders()` (`services/staleOrders.ts`), llamado en `tradingRunner.ts` ANTES del pre-trade check de la Pasada 2, cancela una orden BUY pendiente si (a) lleva más de `pendingOrderTimeoutMin` minutos abierta, o (b) el precio de entrada que se colocaría HOY para ese símbolo ya quedó por debajo del límite de la orden vieja (`freshPrice < order.limitPrice`, sin tolerancia mínima). Las canceladas se sacan de `openOrders` en memoria, así que si la señal sigue BUY ese ciclo, la Pasada 2 normal coloca una orden nueva al precio recalculado - no hay paso de "reemplazo" separado. Toggle: `POST /api/settings/auto-cancel-stale-orders`. El timeout se muestra en el dashboard (tab Operaciones) pero **no es editable desde la UI** - solo vía SQL/API directa.

⚠️ **Bug corregido 2026-06-22** (detectado por las alertas de email de Fase 12 - el usuario reportó "demasiadas notificaciones de compra de AAPL"): la comparación (b) usaba `freshPrice` (float crudo de la estrategia, p.ej. `289.52969999999993`) contra `order.limitPrice` (ya redondeado a centavos por Alpaca/`placeBuyOrder`, `289.53`) - sin redondear `freshPrice` antes de comparar, CUALQUIER precio cuyo 3er decimal redondeara hacia arriba se leía como "el precio bajó" en CADA ciclo, aunque el precio real no se hubiera movido ni un centavo. Resultado en producción: AAPL generó una orden BUY nueva cada 5 minutos durante ~30 min (cancelada+reemplazada en cada ciclo por `cancelStaleOrders`), cada una disparando un email de alerta (Fase 12) - confirmado cruzando `trading_orders`/`ai_assessments` contra el historial real de `GET /v2/orders` de Alpaca (las 6 órdenes canceladas tenían el mismo `limit_price=289.53` y el mismo `queryid` de condición técnica, sin cambio real de precio). Fix: `findStaleOrders` redondea `freshPrice` a centavos (`Math.round(freshPrice * 100) / 100`) antes de comparar - sigue sin tolerancia de PORCENTAJE (la decisión explícita del usuario de 2026-06-21 no se tocó), solo iguala la precisión de la comparación a la que ya tienen los precios reales de Alpaca. Verificado en vivo: tras el fix, el siguiente `npm run trade:cron` reconoció la orden pendiente de AAPL como `PENDING_BUY_ORDER` (bloqueado, sin cancelar/reemplazar) en vez de generar una orden nueva.

⚠️ **Segundo bug corregido 2026-06-23** (el usuario reportó que el bug de arriba volvió a pasar - mismo síntoma, AAPL, distinta causa): el fix del 2026-06-22 arregló el flapping cada 5 min, pero dejó un segundo mecanismo sin tocar - el motivo `'timeout'` de `findStaleOrders` cancelaba una orden BUY pendiente con más de `pendingOrderTimeoutMin` (default 60) minutos SIN mirar si el precio fresco había cambiado. Con una señal BUY "pegajosa" (condición técnica que se sostiene horas porque viene de la vela diaria, que solo se actualiza con la ingesta 3x/día) y un precio estimado que tampoco cambia, esto cancelaba+reemplazaba la orden por una IDÉNTICA cada ~60-65 min durante toda la sesión - confirmado en `logs/trade-cron.log`/`trading_orders` del 2026-06-22: AAPL generó 10 órdenes BUY a $289.53 entre 16:05 y 19:50 UTC, cada una con su propio email (Fase 12) y su propia evaluación de Claude (48 llamadas solo de AAPL ese día, parte del $1.39 gastado). También explicaba las 26 "discrepancia(s) DB vs Alpaca (24h)" del chip del dashboard (`public/app.js` - cada cancelar+reemplazar genera 2 filas transitorias en `sync_discrepancies`, una `pending_order_missing_in_alpaca` para la orden vieja y una `pending_order_missing_in_db` para la nueva, por el desfasaje normal entre el cron de trading y el poller de `operationsSync.ts`) - 24 de las 26 eran ese mismo patrón, las otras 2 eran ruido transitorio normal (una orden de AAPL nueva del día siguiente, un fill de SCHD) sin relación con ningún bug. Fix: `findStaleOrders` ahora solo dispara `'timeout'` si el precio fresco de hoy (redondeado a centavos) es DISTINTO del límite de la orden - si no cambió nada, reemplazarla no aporta nada y se deja pendiente (sigue sin tolerancia de PORCENTAJE). Si el símbolo ya no es candidato BUY este ciclo (`freshPrice` ausente), `'timeout'` sigue disparando igual que antes - ese caso es una orden genuinamente huérfana, no el bucle de clones.

**Decisión explícita del usuario sobre el precio estimado de entrada (2026-06-23)**: en la misma investigación se confirmó que el gap AAPL ($300.89 de precio vs. $289.53 de entrada estimada, -3.78%) viene de `trend_pullback_sma50` (`conditions.ts`, devuelve `sma50[i]` - nivel de indicador, no proyección, ver auditoría Fase 8.1 más abajo) - mecanismo de selección de hoja correcto (`estimateConditionExprEntryPrice` SÍ promedia solo las hojas que dispararon BUY, confirmado leyendo el código), el "problema" es la fórmula de esas 4 condiciones puntuales, ya conocida. El usuario decidió **no tocar la fórmula**: prefiere que el precio de entrada sea el que la condición realmente calcula, aunque la orden nunca se llene por quedar lejos del mercado - motivo explícito: cuando haya varias órdenes llenas y se empiece a usar margen, no quieren pagar precio "premium" por entrar. Lo que sí pidió: que quede claro CUÁL hoja disparó cuando hay 2-3 condiciones (antes `describeConditionExpr` listaba las 3 sin distinguir). Implementado: `describeConditionExpr(node, ctx, i, action)` (`conditionExpr.ts`) antepone `→ ` a la descripción de la hoja cuyo `evaluate(ctx,i) === action` (BUY para el lado compra, SELL para el lado venta) - sin marca si la expresión es trivial (1 sola hoja, sería ruido redundante). `signals.ts` ahora llama `describeConditionExpr` una vez por lado (antes reusaba `buyDetails` para `sellDetails` cuando `buyConditionId === sellConditionId`, porque la marca puede diferir entre lados aunque la expresión sea la misma). Se propaga gratis a `trading_signals.reason`, el dashboard y el texto del email (Fase 12) - **no** al prompt de Claude (`claude.ts#buildPrompt` no usa `reason`, solo `buyConditionLabel`/`sellConditionLabel` sin detalle de indicadores; fuera de alcance de este pedido).

**Tercer fix 2026-06-23** (mismo hallazgo de costo de Claude, causa distinta): `tradingRunner.ts` evaluaba con Claude cualquier señal BUY no bloqueada por clasificación, sin mirar si el símbolo ya tenía posición abierta u orden BUY pendiente - ambos casos los bloquea incondicionalmente `preTradeCheck.ts#computeCanPlaceBuyOrder` (checks 2/3, `POSITION_ALREADY_OPEN`/`PENDING_BUY_ORDER`) DESPUÉS de la evaluación de IA, así que el veredicto de Claude nunca podía cambiar el resultado. Confirmado en producción: SCHD (31 acciones ya compradas el 2026-06-22) siguió generando una evaluación de Claude cada 5 min durante el 2026-06-23 (`ai_assessments`, recommendation `buy-stale`/`buy-fresh`) con una señal BUY técnica que no iba a comprar nada. Fix: el filtro de candidatos BUY (`buyCandidateIndexes`) ahora también excluye símbolos con `positionsBySymbol.has(symbol)` o con alguna orden BUY en `openOrdersBySymbol`, reusando los mapas que la Pasada 2 ya construye - sin llamadas a Alpaca adicionales. El símbolo sigue persistiendo su señal técnica normalmente; solo deja de evaluarse con Claude (queda `not-evaluated` en `ai_assessments`, mismo tratamiento que un símbolo bloqueado por clasificación).

**Cotización en vivo para decisiones de orden (2026-06-23, motivado por el caso GOOGL)**: post-mortem de un trade real con pérdida (-$16.32/-2.30%, 2 acciones @ $354.52 -> @ $346.36) encontró que la orden BUY se llenó en medio de una caída intradía de GOOGL/MSFT de hasta 6% (sell-off de "hiperescaladores de IA", confirmado vía `news_items` - titular "GOOGL, MSFT Plunge Up To 6% On Monday", 2026-06-22) que el bot nunca vio: `signal.price` viene de `market_bars` (vela diaria, refrescada solo 3x/día por `npm run ingest`), así que durante toda la sesión del 06-22 el bot siguió "viendo" $367.93 mientras el precio real ya había caído a ~$354. El `Math.min(estimatedEntryPrice, signal.price)` que ya existía para poner el límite de la orden no protegió nada porque AMBOS lados de ese `min()` eran viejos. Fix: nuevo `getLatestQuotes(client, symbols)` (`services/marketData.ts`) - 1 sola llamada a `GET /v2/stocks/snapshots?symbols=...` (feed=iex, mismo patrón que `getDailyBars`) para TODO el watchlist, devuelve `Map<symbol, latestTrade.p>` (verificado en vivo: 27/27 símbolos, ~435ms). `tradingRunner.ts` la llama UNA vez por ciclo (`livePrices`, fail-open - si falla, Map vacío y cada símbolo cae a `signal.price`, comportamiento idéntico al anterior) justo después de la Pasada 1, y la usa como `livePrice = livePrices.get(symbol) ?? signal.price` en los 3 puntos donde antes se usaba `signal.price` para decidir una orden BUY: `freshEntryPriceBySymbol` (la referencia que usa `cancelStaleOrders`/`findStaleOrders` para el motivo `'price_above_market'`/`'timeout'`, ver arriba), el cálculo de `qty` (`Math.floor(positionValue / livePrice)`, antes `/ signal.price` - tamaño en dólares más preciso), y `entryPrice = Math.min(estimatedEntryPrice, livePrice)` (el límite real que se manda a Alpaca). **Deliberadamente NO toca**: `signals.ts`/`conditions.ts`/`backtest.ts` (cero cambios - los indicadores técnicos siguen viniendo 100% de `market_bars`/velas diarias, confirmado explícitamente con el usuario que esto no debía afectar los cálculos técnicos), `trading_signals.price` persistido (sigue siendo el cierre de la vela diaria, para que sea consistente con los indicadores guardados en la misma fila), el contexto que recibe Claude (`buildPrompt` sigue usando `signal.price` - fuera de alcance de este pedido, que se limitó explícitamente a "al momento de colocar la orden"), y el precio mostrado en el email de la alerta SELL (`signal.price`, sin cambios - un market order no tiene decisión de precio que proteger). Verificado con `npm run trade` real post-fix: ciclo completo sin errores, MSFT generó BUY técnico real y fue vetado por Claude antes de llegar al cálculo de `entryPrice`/`qty` (no se pudo verificar el camino feliz con una compra real en esta corrida, pero la sustitución de variables fue revisada línea por línea y `getLatestQuotes` se probó por separado contra los 27 símbolos).

## Fase 12 (alertas por email, 2026-06-22)

Implementa el punto 14 ("Alertas") del roadmap, acotado a BUY/SELL **realmente ejecutados** (no a toda señal técnica BUY/SELL, ni a `AI_BLOCKED`/ajustes de IA descartados/fallos de IA-ingesta - eso sigue pendiente, ver "Pendientes conocidos").

- `src/config.ts#loadEmailAlertConfig()`: lee `SMTP_HOST/SMTP_PORT/SMTP_SECURE/SMTP_USER/SMTP_PASSWORD/SMTP_FROM/ALERT_EMAIL_TO` de `secure/keys.env`. Devuelve `null` (no lanza) si falta `SMTP_HOST`/`SMTP_USER`/`SMTP_PASSWORD`/`ALERT_EMAIL_TO` - a diferencia de `loadAlpacaConfig`/`loadAnthropicConfig` (que lanzan si falta algo requerido), este es un feature opt-in: sin configurar, el ciclo de trading sigue funcionando igual, simplemente sin enviar emails. Configurado en producción con Gmail SMTP (contraseña de aplicación) desde 2026-06-22.
- **Contenido enriquecido** (ampliación same-day, 2026-06-22): el email es HTML (con fallback `text` plano) y, por cada orden ejecutada, incluye condición de compra/venta activa (`signal.buyConditionLabel`/`sellConditionLabel`), `signal.reason`, indicadores (SMA rápida/lenta, RSI, momentum), precios estimados de entrada/salida, la evaluación de IA de Claude de ESE mismo ciclo si la hubo (`recommendation`/`score`/`confidence`/`rationale` - hasta 2026-06-22 solo BUY, desde 2026-06-23 también SELL cuando hubo evaluación, ver "Fase 11") y un gráfico PNG adjunto (inline, `cid:`) con el mismo look que la sección "Detalle" del dashboard.
- `src/strategy/chartOverlays.ts` (puerto a TypeScript de `CONDITION_CHART_CONFIG`/`mergeConditionChartConfig` de `public/app.js`, lógica pura): **duplicado deliberado** porque el frontend es JS plano sin bundler (no hay módulo compartible entre browser y Node) - si se cambian los overlays en `app.js`, replicar el cambio acá a mano.
- `src/services/chartImage.ts` (`renderSymbolChartPng(points, buyConditionId, sellConditionId, estimatedEntryPrice, estimatedExitPrice) -> Promise<Buffer>`): renderiza a PNG (800×420, tema oscuro igual al dashboard) vía `chartjs-node-canvas` (Chart.js v4 + `canvas` nativo) - mismos overlays que `mergeConditionChartConfig` arroje, con los 3 toggles del dashboard (MA/Bollinger/oscilador) siempre en `true` (no hay estado de sesión en un email estático).
- **Dependencia nativa `canvas`** (vía `chartjs-node-canvas`): requirió instalar paquetes apt de sistema - `libcairo2-dev`, `libpango1.0-dev`, `libjpeg-dev`, `librsvg2-dev`, `libgif-dev` (2026-06-22, **con confirmación explícita del usuario**, ver regla 4 de este archivo - la pregunta de qué estrategia de render usar fue presentada como ese pedido explícito antes de instalar nada).
- `src/services/email.ts`: `TradeAlertEntry` (`{type: 'BUY'|'SELL', symbol, qty, price, orderId, signal: SignalResult, bars: OhlcBar[], ai: {recommendation, score, confidence, rationale} | null}`), `sendTradeAlertEmail(config, entries: TradeAlertEntry[])` - un solo email agrupa TODAS las entradas (asunto `Vibe Bots: N BUY / M SELL`), un bloque HTML + un gráfico por entrada (`buildChartSeries(entry.bars)` + `renderSymbolChartPng`), separados por `<hr>`. Si el render del gráfico de una entrada falla, ese bloque se manda sin imagen (try/catch por entrada, no aborta el email completo).
- `tradingRunner.ts`: Pasada 1 ahora guarda `barsBySymbol: Map<string, OhlcBar[]>` (las mismas velas ya pedidas para calcular la señal, reusadas para el gráfico - sin un segundo round-trip a la DB). En las dos ramas que colocan una orden real (`OPEN_POSITION` tras `placeBuyOrder`, `CLOSE_POSITION` tras `closePosition`) se agrega una entrada a `tradeAlertEntries` (acumulador local, no persiste en DB) con `signal`/`bars`/`ai` (`ai` se llena con el `assessment` en memoria de ESE ciclo si lo hubo - en BUY desde el origen de esta fase, en SELL desde 2026-06-23 - nunca desde `ai_assessments` persistido; `null` si no hubo evaluación). Al final del ciclo (después de esperar `claudeExperimentPromise`, antes del snapshot de MinIO), si `tradeAlertEntries.length > 0`, intenta `loadEmailAlertConfig()` + `sendTradeAlertEmail()` en un try/catch que solo loguea (`console.error`) - igual patrón fail-open que el snapshot de MinIO, nunca rompe `runTradingCycle()`.
- Un solo email agrupa TODAS las órdenes ejecutadas en ese ciclo (si varios símbolos dispararon BUY/SELL en la misma corrida de 5 min, es un email, no varios) - decisión deliberada para no generar spam en una cadencia de cron de 5 minutos.
- **No envía nada** si: no hay `OPEN_POSITION`/`CLOSE_POSITION` ese ciclo (lo normal, la mayoría de ciclos no ejecutan órdenes), SMTP no está configurado, o `settings.tradingEnabled = false` (no llega a haber órdenes que generen entradas).
- Variables agregadas a `.env.example` y a `secure/keys.env` (este último con `SMTP_HOST/SMTP_USER/SMTP_FROM/ALERT_EMAIL_TO` ya completados con la cuenta Gmail real del usuario - solo `SMTP_PASSWORD` quedaba pendiente, completado 2026-06-22 con una contraseña de aplicación).
- Verificado end-to-end 2026-06-22: `npm run test-email` ([src/testEmailAlert.ts](src/testEmailAlert.ts), datos sintéticos) confirmó SMTP + render de gráfico + entrega real a la bandeja de entrada.

## Fase 13 (monitoreo de infraestructura, 2026-06-22)

`grafana-server` ya corría nativo con un datasource Postgres y 2 dashboards de negocio (`vibe-overview`, `vibe-trading`, ver "Pendientes conocidos" - su bug de paneles `timeseries` sin datos NO se tocó en esta fase) pero sin ninguna métrica de infraestructura. El usuario eligió explícitamente **revivir Grafana** (vs. construir gráficos nativos dentro del propio dashboard de vibe-bots) - decisión deliberada que reabre algo que se había dejado de documentar a propósito el 2026-06-21 ("ya no la usamos"), ahora con un propósito distinto (infra, no los dashboards de negocio rotos).

**Stack instalado** (apt + systemd, mismo patrón nativo que Postgres/Redis/MinIO/Grafana - confirmado explícitamente por el usuario antes de instalar, ver regla 4):
- `prometheus` (puerto 9090, `/etc/prometheus/prometheus.yml`, `scrape_interval: 15s` ya viene por defecto en el paquete Ubuntu) - jobs `prometheus` (sí mismo), `node` (9100, viene por defecto), `postgres` (9187, agregado), `redis` (9121, agregado). Retención default 15 días (hay 83GB libres - subible si se quiere más historia, no se cambió en esta fase).
- `prometheus-node-exporter` (9100): CPU/RAM/disco/red de la VM. Textfile collector activado (`ARGS="--collector.textfile.directory=/var/lib/prometheus/node-exporter"` en `/etc/default/prometheus-node-exporter`, no viene activado por defecto) para 2 métricas custom: `vibebots_minio_bucket_bytes` (`du -sb /var/lib/minio/vibe-bots`) y `vibebots_logs_dir_bytes` (`du -sb logs/`), pobladas por `monitoring/collect-vibebots-metrics.sh` (escritura atómica - tmpfile + `mv` - para que node_exporter nunca lea un `.prom` a medio escribir) cada 5 min vía una entrada nueva en el crontab de `root` (mismo mecanismo que `trade:cron`/ingesta/watchdog, ver "Automatización" - no un systemd timer, por consistencia).
- `prometheus-postgres-exporter` (9187): `DATA_SOURCE_NAME` en `/etc/default/prometheus-postgres-exporter` (no versionado, tiene la password real de `vibe_bot`) reusa el rol `vibe_bot` (no se creó un rol de solo-lectura dedicado - decisión deliberada, `vibe_bot` ya es no-superusuario). Métricas de fábrica: `pg_database_size_bytes`, `pg_stat_database_numbackends` (conexiones), `pg_settings_max_connections`, `pg_stat_database_blks_hit`/`blks_read` (cache hit ratio), `pg_stat_user_tables_n_dead_tup` (autovacuum). Collector `wal` deshabilitado (`--no-collector.wal` en `ARGS`) porque `vibe_bot` no tiene permiso para `pg_ls_waldir()` (logueaba un error por scrape, sin romper el resto). Query custom (`--extend.query-path=/etc/prometheus-postgres-exporter/queries.yaml`, mecanismo marcado DEPRECATED upstream pero funcional en la versión empaquetada 0.15.0) vía `monitoring/postgres_exporter_queries.yaml` (versionado en el repo, copiar a esa ruta en la VM si se reinstala):
  - `pg_vibebots_table_sizes_size_bytes{table=...}`: tamaño de `market_bars`, `trading_signals`, `trading_orders`, `claude_usage_log`, `claude_gate_experiment`, `ai_assessments`.
  - `pg_stat_statements_top_*`: top 20 queries por `total_exec_time` desde `pg_stat_statements` (`calls`, `total_exec_time`, `mean_exec_time`, `rows`, `shared_blks_hit`/`shared_blks_read` - esto es la latencia/IO real por query que pedía el usuario, postgres_exporter NO trae un collector de `pg_stat_statements` de fábrica). Una fila aparece con `query="<insufficient privilege>"` para statements de otros roles (p.ej. `postgres` superuser) - normal, `pg_stat_statements` enmascara el texto de queries de roles que no sean el que consulta.
- **`pg_stat_statements`**: `shared_preload_libraries = 'pg_stat_statements'` en `/etc/postgresql/16/main/postgresql.conf` (estaba vacío) + `systemctl restart postgresql` (corte de pocos segundos, **confirmado explícitamente por el usuario** de antemano) + `CREATE EXTENSION IF NOT EXISTS pg_stat_statements;` en la DB `vibe`. Verificado post-reinicio: `GET /api/health` siguió devolviendo `postgres: ok` y el proceso del dashboard web no se cayó (el pool de `pg` reconectó solo) - sin acción manual necesaria.
- `prometheus-redis-exporter` (9121): cero configuración - default `redis://localhost:6379` ya matchea `REDIS_URL` (sin password en este setup). Sumado a esta tanda a pedido explícito del usuario (no era parte del pedido original de hardware+DB).
- Dashboard nuevo **"Vibe Bots - Infra"** (`grafana/dashboards/vibe-infra.json`, uid `vibe-bots-infra`, mismo patrón que `vibe-overview.json`/`vibe-trading.json` - JSON "pelado" sin el wrapper `{dashboard:...}` de la API), datasource Prometheus (uid `bfpx1cj0au22od`, creado vía `POST /api/datasources`, no provisionado por archivo) - 4 filas (Hardware/PostgreSQL/Redis/Espacio de vibe-bots), 10 paneles `timeseries` + 1 `table` (top 10 queries), todos `gridPos.w: 24` (ancho completo). **Verificación real, no solo "el panel existe"**: cada query se corrió vía `POST /api/ds/query` (proxy de Grafana hacia Prometheus) con un rango de 6h y se confirmó al menos 1 punto de dato no vacío por panel - relevante porque `vibe-overview`/`vibe-trading` (Postgres-backed) tienen un bug histórico de paneles sin datos (ver "Pendientes conocidos"); los paneles Prometheus-backed de esta fase no comparten esa causa raíz (queries PromQL, no SQL crudo) y se confirmó explícitamente que no la repiten.
- **Consolidación de paneles** (ampliación same-day, 2026-06-22 - versión `vibe-infra.json` v2, mismo `uid` así el link público no cambia): el usuario pidió agrupar métricas relacionadas (1-3 paneles por categoría, no 1 por métrica). Técnica: paneles con 2 unidades distintas usan `fieldConfig.overrides` con matcher `byFrameRefID` (apunta TODO un target/refId, no series individuales por nombre) + `custom.axisPlacement: 'right'` para mandar ese grupo de series al eje derecho - mismo panel, 2 escalas independientes. Cambios concretos: "CPU" ahora es 1 panel con uso total + uso **por core** (`rate(node_cpu_seconds_total{mode="idle"}[5m])` SIN agregación `avg`/`sum` preserva la label `cpu`, 4 series vía `{{cpu}}`) + load average 1/5/15m en eje derecho (antes 2 paneles separados, sin desagregación por core); "Disco I/O" + "Disco IOPS" se fusionaron en 1 panel (bytes/s eje izq., ops/s eje der., antes 2 paneles); "Tamaño DB" + "tamaño de tablas" en 1 panel (mismo eje, mismo unit `bytes`, sin necesidad de override); "Cache hit ratio" + "dead tuples" en 1 panel (eje derecho para dead tuples); Redis "memoria + clientes" y "hit/miss ratio + evictions" en 2 paneles (antes 4); "MinIO bucket" + "logs/" en 1 panel (Espacio de vibe-bots). Resultado: 18→11 paneles `timeseries` (la tabla de top 10 queries no se tocó, no es candidata a fusión). Verificado con `POST /api/ds/query` (igual chequeo que la v1) + captura de pantalla real (Playwright contra la Public Dashboard URL, sin necesitar login de Grafana) confirmando visualmente que los ejes duales se ven correctos.
- `scripts/status.sh`: agrega `prometheus`, `prometheus-node-exporter`, `prometheus-postgres-exporter`, `prometheus-redis-exporter` al loop de `systemctl is-active` (ensanchado el padding de `%-16s` a `%-30s` para que los nombres largos no rompan la alineación).

**Embebido en el dashboard de vibe-bots** (ampliación same-day, 2026-06-22 - el usuario pidió verlo en el front-end propio, no solo abrir Grafana aparte): se restauró el mecanismo de iframe que existía antes de quitarse el 2026-06-14 (`grafanaContainer`/`loadGrafana()` en `public/app.js`, leyendo `GET /api/config` → `config.grafanaPublicUrl`, ya expuesto en `server.ts` sin cambios) - código casi idéntico al original (recuperado de `git log -p` sobre el commit que lo quitó, `084887e`). `GRAFANA_PUBLIC_URL` (`secure/keys.env`) se REPUNTÓ de la Public Dashboard de "Overview" (`accessToken=310687db...`, sigue existiendo, solo dejó de embeberse) a una Public Dashboard nueva creada para "Vibe Bots - Infra" (`POST /api/dashboards/uid/vibe-bots-infra/public-dashboards`, `timeSelectionEnabled: true` - a diferencia de la de "Overview" que tiene `false` - para poder cambiar el rango de tiempo desde el iframe mismo). Requirió reiniciar el proceso web (`stop-web.sh`+`start-web.sh`, nunca systemd - regla 1) para que `dotenv` recargara el nuevo valor.
- **Tab propio "Sistema-HWM"** (ampliación same-day, 2026-06-22 - el usuario lo quería separado de "Sistema", no anidado): `public/index.html` agrega un 6º tab (`data-tab="sistema-hwm"`) y un `tab-panel` propio (`id="tab-sistema-hwm"`) con la card "Infraestructura (HWM)" - se sacó por completo de `tab-sistema` (que vuelve a tener solo "Estado de servicios" seguido de "Ingesta de datos"). `app.js#activateTab` ya era genérico por `data-tab`/`id`, cero cambios de JS de routing. CSS: `#grafana-container iframe` con `height: 3900px` (recalculado tras la consolidación de paneles - ver bullet siguiente - para que el dashboard completo entre sin scroll interno).
- **Consolidación de paneles v2 (mismo día)**: el usuario pidió 1) fusionar "Disco usado %" dentro del panel de I/O+IOPS (ahora 3 ejes en 1 panel: `custom.axisPlacement` izq.=`percent` default, der.=`Bps` para I/O y der.=`short` para IOPS - Grafana apila más de un eje del mismo lado si difieren en `unit`, confirmado visualmente) y 2) que todos los paneles ocupen el ancho completo (`gridPos.w: 24` en vez de `12`, layout a 1 columna en vez de 2) en lugar de un ancho fijo de mitad de página. Resultado: `vibe-infra.json` pasó de 11→10 paneles `timeseries` (el de "Disco usado %" se fusionó en el de I/O+IOPS), + 1 `table` sin cambios, todos `w:24` ahora, version `3` del dashboard (mismo `uid`, el link público no cambió). Verificado con `POST /api/ds/query` + captura de pantalla (mismo flujo de Playwright que las verificaciones anteriores) confirmando los 3 ejes del panel de disco y el ancho completo de cada panel.

**Fuera de alcance, a propósito**: no se arregló el bug histórico de `vibe-overview`/`vibe-trading`; no se creó un rol Postgres de solo-lectura dedicado para el exporter; no se mide `node_modules`/`dist` (artefactos de build, no datos operativos); no se subió la retención de Prometheus por encima del default de 15 días.

## Snapshots en MinIO (Fase 3)

- `src/services/storage.ts`: además del health-check, expone `putJsonSnapshot`, `listSnapshots`, `getSnapshotStream`.
- `runIngest()` y `runTradingCycle()` suben, al final de cada corrida, un snapshot JSON **best-effort** (no rompe la corrida si MinIO falla, ver `snapshotKey: string | null` en `IngestSummary`/`TradingCycleResult`): `ingest/<ts>.json` (`{generatedAt, watchlist, macroSeries, bars, news, fundamentals, macroObservations, quotes}`) y `trading/<ts>.json` (`{generatedAt, account, signals, actions}`). `<ts> = new Date().toISOString().replace(/[:.]/g, '-')`.
- `server.ts` expone `GET /api/snapshots` (lista hasta 30, ingesta+trading mezclados) y `GET /api/snapshots/download?key=...` (valida `^(ingest|trading)/[A-Za-z0-9_\-:.]+\.json$` contra path traversal). Sección "Snapshots (MinIO)" en `public/` (tabla con descarga).
- Backup de PostgreSQL a MinIO (`pg_dump`) queda diferido - requiere su propia decisión de scheduling (cron), fuera de esta fase.

## Comandos

`npm run build` (tsc) · `npm run dev` (diagnóstico) · `npm run ingest` · `npm run trade` · `npm run trade:cron` (wrapper cron-safe, ver abajo) · `npm run backtest` (Fase 4, legacy/sin grupo - ver "Fase 10" para la variante segmentada, solo vía API) · `npm run backfill-history` (opcional, Fase 4, no corrido aún) · `npm run backfill-1h` (opcional, Fase híbrido, no corrido aún) · `npm run test-email` (Fase 12, envía un email de prueba con `sendTradeAlertEmail` para verificar `SMTP_*`/`ALERT_EMAIL_TO` sin esperar un BUY/SELL real) · `npm run web:start` / `web:stop` / `status` (dashboard, scripts/, NUNCA systemd).

## Automatización (cron, desde 2026-06-14 - ⚠️ cadencia real verificada 2026-06-21 vía `crontab -l`, distinta de lo que decían versiones anteriores de esta sección)

- `src/cronTrade.ts` (`npm run trade:cron`) llama a `getMarketClock` (`GET /v2/clock` de Alpaca, `src/services/alpaca.ts`) y solo ejecuta `runTradingCycle()` si `isOpen`; si el mercado está cerrado, loguea `nextOpen` y sale 0 (no-op, seguro para cron).
- **Crontab de `root`** (fuera del repo, no versionado - revisar con `crontab -l` tras una auto-compactación; cada bloque tiene comentarios propios en el crontab, algunos también desactualizados - confiar en el campo cron, no en el comentario):
  - Ciclo de trading: `*/5 13-21 * * 1-5` (**cada 5 minutos**, no cada hora - el comentario del crontab dice "cada 30 min", también desactualizado). `/v2/clock` filtra fuera de sesión/feriados/fines de semana, así que en la práctica es un no-op fuera de horario de mercado real.
  - Ingesta: `0 12,16,22 * * 1-5` (**3 veces al día**: 12:00 UTC pre-apertura, 16:00 UTC mediodía, 22:00 UTC post-cierre - no solo pre/post como decían versiones anteriores de esta nota).
  - Watchdog del dashboard: `*/5 * * * *` (todos los días) - `bash scripts/start-web.sh` (idempotente vía PID file), reinicia el dashboard si se cayó. Log en `logs/watchdog.log`.
  - Métricas custom de espacio (Fase 13): `*/5 * * * *` (todos los días) - `monitoring/collect-vibebots-metrics.sh` (tamaño de bucket MinIO/`logs/` para el textfile collector de `node_exporter`). Log en `logs/metrics-collector.log`.
- ⚠️ **Implicancia de costo de Claude por la cadencia real de 5 min**: la fase de IA (Fase 11) ya no tiene gate de "1 vez por día" (se quitó junto con el filtro de candidatos original, ver "Fase 11" - el viejo gate de Redis `claude:last_run_date` ya no existe). Como las señales técnicas vienen de barras **diarias** que no cambian intra-día, un símbolo con una condición BUY o SELL "pegajosa" (p.ej. breakout/pullback que se mantiene varios ciclos, o un veto de IA que no cambia mientras la posición sigue abierta) puede terminar evaluado por Claude en CADA corrida de 5 min mientras siga siendo candidato y no se haya bloqueado/comprado/cerrado - no hay deduplicación dentro del mismo día más allá del filtro de candidatos (posición/orden pendiente para BUY, posición abierta para SELL). Confirmado empíricamente 2026-06-22/23 con AAPL (48 llamadas BUY en un día) y SCHD (evaluaciones BUY repetidas con posición ya abierta, motivo del fix del punto 1 de "Fase 11") - ver "Pendientes conocidos". Si el costo termina siendo alto, la corrección sería un throttle de "no re-preguntar lo mismo en una ventana corta" - **no** un corte automático por presupuesto (eso está explícitamente prohibido, ver "Fase 11").
- Logs en `logs/trade-cron.log` / `logs/ingest-cron.log` (gitignored).

## Pendientes conocidos / diferidos

- Grafana: los 2 dashboards de negocio LEGACY (`vibe-overview`, `vibe-trading`, Postgres-backed) siguen con paneles `timeseries` que no muestran datos visualmente (`fieldConfig.defaults.custom`, v2) - bug histórico, NO se tocó en la Fase 13. El iframe de "Vibe Bots - Overview" se quitó del dashboard web (2026-06-14). **Actualización 2026-06-22 (Fase 13)**: la nota de 2026-06-21 ("README ya no documenta Grafana, ya no la usamos") quedó obsoleta - el usuario decidió explícitamente revivir Grafana con un propósito nuevo (monitoreo de infra vía Prometheus, dashboard "Vibe Bots - Infra", ver "Fase 13"), ya documentado de nuevo en `README.md`. Lo que sigue pendiente es específicamente arreglar (o formalmente discontinuar) los 2 dashboards de negocio legacy - no Grafana en general.
- Orden de compra LÍMITE puede no ejecutarse si el precio nunca toca `estimatedEntryPrice` en la sesión (`time_in_force: 'day'`).
- Fase 4 - capa de IA (Claude): activa desde 2026-06-14 (`ANTHROPIC_API_KEY` en `secure/keys.env`, ver "Capa de IA (Claude, Fase 4/5)"). Verificar periódicamente que el check `anthropic` siga ✅ y que `ai_assessments`/`AI_BLOCKED` sigan apareciendo cuando corresponda.
- `npm run backfill-history` (Fase 4) está disponible pero no se ha corrido - `npm run backtest` usa por ahora solo el histórico de ~150 velas de la ingesta normal. Tampoco se corrió `npm run backfill-1h` (Fase híbrido).
- **Ruteo multi-cuenta sin completar** (Fase 10): `account_group` solo etiqueta, todas las órdenes reales van a la cuenta única de `ALPACA_API_KEY`. `ALPACA_BLOQUEADOS_*` no está configurada. Decidir si se completa el ruteo o se formaliza el modelo actual (1 cuenta de ejecución + vistas multi-cuenta de solo lectura).
- **`bot_settings.pending_order_timeout_min`** (Fase 11) no es editable desde el dashboard, solo vía SQL/API directa.
- **Endpoints de backtest duplicados**: `/api/backtesting/*` (legacy, sin grupo) y `/api/backtest/*` (Fase 10, con grupo) coexisten sin que ninguno se haya deprecado.
- **Costo no acotado de la fase de IA, confirmado con volumen real** (Fase 11, ver "Automatización"): sin el viejo gate de 1-vez-por-día, y con el cron de trading corriendo cada 5 min, un candidato persistente (BUY o, desde 2026-06-23, también SELL) puede generar muchas llamadas a Claude el mismo día. Confirmado en `claude_usage_log` el 2026-06-22 ($1.39, 460 llamadas - 79 producción + 381 experimento - solo de ese día, con AAPL solo generando 48 de las 79 de producción). Las exclusiones agregadas el 2026-06-23 (posición abierta/orden pendiente para BUY, posición requerida para SELL, ver "Fase 11" puntos 1/4) reducen el caso más extremo (SCHD comprado evaluándose igual cada 5 min) pero NO eliminan el patrón general - un candidato BUY sin posición/orden aún, o un candidato SELL vetado por la IA (que por diseño no tiene throttle, ver punto 4 de "Fase 11"), sigue sin deduplicación dentro del día. Si el costo sigue siendo alto, la corrección sería un throttle de "no re-preguntar lo mismo en una ventana corta" - **no** un corte por presupuesto (prohibido explícitamente, ver "Fase 11").
- **Esta misma sección de `CLAUDE.md` (y las secciones "Fase híbrido"/"Fase 8"/"Fase 9") estaban desactualizadas hasta esta revisión** (2026-06-21) - "Fase híbrido" describía una orquestación de señales 1H como activa cuando no lo está, y "Fase 8"/"Fase 9" decían "no mergeada" estando ya mergeadas hace tiempo. Si se vuelve a auto-compactar sin que un agente haga este tipo de verificación cruzada contra `git log`/el código, esta deriva puede repetirse - considerar un recordatorio en el flujo de PR/merge para actualizar `CLAUDE.md`/`README.md` (ver "Próximas fases" de `README.md`).
